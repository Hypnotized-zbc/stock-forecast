# 更新报告

## v0.25.0 (2026-08-14)
- 英文模式下中文残留清理（用户反馈：查询栏"共242个交易日"、对比窗口"可用范围"等）：
  - 当前查询股票副标题"共 N 个交易日" → I18N 双语（英文 "{n} trading days"）
  - 状态栏周期标签与文案（日K/周K/月K、数据已加载、数据已加载（缓存）、数据获取中、日K聚合兜底）全部双语
  - 多股对比窗口"可用范围"、候选"当前股票"双语；英文界面半角标点
  - 分享卡片"最新收盘/涨跌幅/个交易日/页脚免责/文件名行情卡片_"双语（英文 stock-card_）
  - 排行榜底部"共 N 只"双语
- 排行榜/资金流渠道修复（用户反馈：经常报错无法查询到数据源且无可用缓存）：
  - 数据源超时加速：东财 retries 3→1、timeout 8→5s（快速失败切新浪）；新浪 timeout 8→6s
  - 磁盘缓存兜底增强：接口全挂时返回过期缓存（标注 cached + stale + 更新于 xx:xx），不再报"无可用缓存"；
    读取过期缓存不再删除文件（删除交给写入覆盖与启动清理），保证兜底能读到
  - 路由层捕获 ConnectionError → 返回错误码（lb_unavailable / ff_unavailable，503），前端映射双语文案
    （英文 "Leaderboard data source unavailable and no cache"）
  - 实测：东财 CLIST 持续失败时自动切新浪返回 100 条真实数据；全挂时返回 503 错误码而非 500 崩溃页
- AI 解读支持英文模式（用户反馈：AI 解读也要切换成英文）：
  - 后端 ai_insight 加 lang 参数：英文界面用英文 prompt 输出英文解读（趋势/支撑压力/量价/风险）；
    /api/insight 路由读 lang
  - 前端请求带 lang=_lang；AI 缓存 key 与云端 period 字段加语言段（day_zh / day_en），中英文解读互不串号
  - 实测：lang=en 输出纯英文（0 中文字符），默认输出中文
- 验证：静态断言 23 项全过 + 运行时（排行榜新浪兜底/当日磁盘缓存/过期缓存 stale 兜底/错误码 503/AI 英文/中文）
  + Edge headless CDP 端到端（英文副标题 "30 trading days"、对比窗口 "Available range"、中文字典正确）

## v0.24.0 (2026-08-14)
- 顶部导航栏改两行布局（用户反馈：英文模式下四个大模块英文较长，一行放不下）：
  - 第一行：标题 + 语言切换 / 登录；第二行：行情分析 / 排行榜 / 资金流向 / 历史统计 四个模块
  - 中英文两个界面统一两行结构，英文模块名再长也不挤压错位
- 英文模式下模型拟合 / 未来预测面板及图表中文残留修复（用户反馈：右侧解释字样与坐标轴区域仍显示"平均绝对误差"等中文）：
  - 拟合面板指标行：MAE 平均绝对误差 / RMSE 均方根误差 / R² 拟合度 → I18N 双语（英文 Mean Absolute Error / Root Mean Square Error / Fit），英文界面半角冒号
  - 拟合图 / 拟合面板图例模型名：英文界面统一映射（ARIMA(1,1,0)+drift / ETS(α=0.30) / Prophet(light) / SVR(RBF) / RF(light)），旧缓存中文名自动兜底；中文界面保持后端原名
  - 未来预测面板：模型图例（ARIMA Forecast / ETS Forecast / Prophet Forecast / SVR Forecast / RF Forecast）、最终预测（加权平均，粗线）、上涨 ↑ / 下跌 ↓、10日后 → 全部 I18N 双语，英文界面半角标点
  - 画布空态提示"请先在上方输入股票名称查询" → t("queryFirst") 双语
- 验证：Edge headless CDP 端到端（中英文两行布局 rect 断言、英文 fit/future 面板无中文残留、中文界面保持中文标签、K线画布真实绘制非空白）+ 静态断言 17 项全过

## v0.23.3 (2026-08-13)
- 资金流 / 排行榜磁盘缓存改为「当日缓存优先」（用户反馈：资金流向每天都一样，
  不用重复获取）：
  - 今天获取过的资金流/排行榜直接返回磁盘缓存，不再请求数据源（内存 60s 缓存保留）
  - 跨天自动过期：读取时发现过期即删除文件，重新请求并落盘（每日更新）
  - 启动时 _disk_cache_cleanup() 清理超过 1 天的缓存文件，防磁盘冗余
  - 删除后台 2 小时刷新循环（当日缓存优先后无意义）；启动预取保留（仅当日无缓存时生效）
- 删除排行榜「量比榜」选项（用户反馈：永远加载不出来——新浪源无量比字段、
  东财接口不稳定）：前端 tab 与 I18N key 移除；表格「量比」列保留（其他榜东财源有值）；
  后端 kind=volratio API 支持保留（无入口）
- 验证：ad-hoc 脚本 9/9 —— 当日缓存优先（mock 网络全挂仍返回缓存、网络 0 请求）、
  跨天过期（旧文件删除+重新请求）、预取循环/2h 循环、I18N 一致且 lbVolratio 已删

## v0.23.2 (2026-08-13)
- 自选股加入入口统一（用户要求）：
  - 行情分析页候选旁的 ＋ 号按钮改为文字「加入自选股」；引导文案/空态文案同步更新
  - 资金流向 / 历史统计板块的查询候选同样新增「加入自选股」文字按钮
    （doStockSearch 通用渲染），点击与行情分析页一致：本地 _watch + 云端
    cloudPost /api/watchlist 双写
- 修复竞态：loadWatchFromCloud 云端返回时直接覆盖 _watch，会丢掉
  「云端响应慢期间用户刚点加入的自选股」——改为云端列表 + 本地新加合并（按 secid 去重）
- 验证：Edge headless CDP 三板块候选按钮渲染（分析/资金流/统计各 1 个）、
  资金流板块点击「加入自选股」→ 快捷列表出现 + 服务端 watchlist 数据库落库
  （1.601088 中国神华 / 0.000858 五粮液 端到端确认）

## v0.23.1 (2026-08-13)
- 信息源多级降级（用户反馈：资金流向/排行榜仍显示"数据源请求失败且无缓存"）：
  - 排行榜三级：东财 clist（主，含量比/PE/PB）→ 新浪 Market_Center.getHQNodeData（备）→
    磁盘缓存（兜底）。新浪源字段统一单位：volume 股→手、mktcap 万元→元；量比榜新浪无
    此字段，仅东财+缓存
  - 资金流三级：东财 fflow 日K（主）→ 新浪 MoneyFlow.ssl_qsfx_lscjfb（备）→ 磁盘缓存。
    新浪分单字段完整（netamount 主力 / r0_net超大 / r1_net大 / r2_net中 / r3_net小，
    与东财同构且 r0+r1+r2+r3=netamount 已校验）
  - 前端标题显示数据源标记（新浪源时显示 [新浪源]）；cached 时显示缓存更新时间
- 主动缓存（用户建议：单独缓存地方放每日更新的数据）：
  - 启动后后台预取排行榜 5 类缓存（东财失败自动走新浪，失败静默不影响启动）
  - 后台线程每 2 小时刷新排行榜缓存，覆盖每日数据更新
  - 磁盘缓存在 data/api_cache/（与代码分离，不推 GitHub）；成功获取即落盘，
    接口全挂时返回上次成功数据，不再空白
- 验证：新浪接口实测（排行 94 条/资金流 10 条+分单 sum 校验）、mock 东财失败→新浪降级
  （source=sina）、启动预取自动生成 lb_*.json、I18N zh/en 219 key 一致、JS/PY 语法

## v0.23.0 (2026-08-13)
- 删除预测战绩功能及板块（用户反馈：没用）：
  - 前端：导航栏按钮、page-predict 板块、loadPredictLog/renderPredictLog/renderPrDetail
    等 JS、pr* 系列 CSS 与 I18N key 全部移除
  - 后端：/api/predict-log 路由、weighted_predict/_log_predict/_settle_predictions、
    FIT_ORDER 删除；/api/kline 不再写预测记录
  - 数据库：db.py 移除 predict_log 建表与函数（已有库中残留表不影响运行，不再写入）
- 资金流向 / 历史统计板块新增股票查询 + 自选股列表（用户要求）：
  - 两板块顶部各加搜索框（复用 /api/search，通用 doStockSearch 函数）+ 自选股快捷列表
  - 资金流板块点击自选股行 → 直接加载该股资金流；统计板块点击 → 加载K线并渲染统计
  - 自选股列表与行情分析页共用 _watch 数据源，登录加载/增删自选后自动同步
- 信息源查询优化（用户反馈：资金流向板块经常数据源接口请求失败）：
  - 资金流 / 排行榜：60 秒内存缓存之外新增磁盘缓存兜底（data/api_cache/，
    成功获取即落盘；接口失败时返回上次成功数据并标注 cached=true + 缓存时间，
    前端标题显示"（缓存数据 · 更新于 xx:xx）"）
  - 完全无缓存且接口失败时：前端显示"数据源请求失败且无缓存" + 重试按钮，不再白屏
- 验证：predict-log 404、I18N zh/en 218 key 一致、JS/PY 语法、缓存兜底端到端
  （造缓存后接口失败返回 cached=true）、Edge headless CDP 渲染验证
  （4 板块导航、资金流/统计自选股列表各 2 行、搜索候选、失败提示+重试按钮、
  统计点击自选股加载 243 日数据并渲染 5 卡片）

## v0.22.0 (2026-08-13)
- 顶部导航栏多板块（用户要求：标题/登录做成栏目，板块点击切换）：
  - 5 个板块：行情分析（原有全部功能不变）/ 排行榜 / 资金流向 / 历史统计 / 预测战绩
  - 板块切换只隐藏/显示对应 section，行情分析页所有交互原样保留；切回时自动重绘当前图表
- 预测回测闭环（预测不再自说自话，自动结算对账）：
  - 新增 predict_log 表（v0.22.0 自动建表，幂等）：登录用户查询K线即落一条预测记录
    （同日同股只保留最新，每用户每股保留最近 20 条）
  - /api/predict-log 自动结算：对已到期预测日拉实际收盘，计算 MAE/RMSE、
    方向命中率（逐日方向对比）、平均误差（带符号，看系统性偏大/偏小）
  - 预测战绩页：汇总卡片（总记录/已结算/待结算/平均MAE/方向命中率/平均误差）+
    记录表格（状态/已结算天数/末日预测/末日实际/MAE/方向命中）+ 点击行展开逐日
    预测 vs 实际对比表 + "查看"按钮跳转行情分析；"立即结算"手动触发
  - 后端 weighted_predict 与前端 weightedPredict 同公式（RMSE 逆加权），保证一致
- 排行榜（东财 clist 接口，60 秒缓存防限流）：
  - 涨幅榜/跌幅榜/成交额榜/换手率榜/量比榜 5 个 tab，一次拉前 100
  - 表格列全保留：排名/名称/最新价/涨跌幅/涨跌额/成交量/成交额/换手率/量比/PE/PB/总市值
  - 点击行跳转行情分析打开该股
- 资金流向（东财 fflow 接口，60 秒缓存）：
  - 个股主力/超大/大/中/小单净流入，近 10/30/60/120 日可选
  - Canvas 柱状图（正=净流入红柱、负=净流出绿柱，零轴 + 亿单位刻度）+ 全列数据表
  - 自带搜索（复用 /api/search），也可自动跟随行情分析当前股票
- 历史统计（纯前端计算，零新接口，复用已有K线数据）：
  - 5 张指标卡片：区间涨跌幅/年化收益率/年化波动率/夏普比率(无风险2%)/最大回撤
  - 14 列完整统计表（含上涨/下跌天数及占比、最大单日涨跌、平均/中位数/标准差日涨幅）
  - 日收益率分布直方图（Canvas，24 桶，正收益红/负收益绿）——保留全部数值列，不做丢数据热力图
- 前端格式化：新增 fmtSigned（带符号金额，亿/万、英文 B/M/K）与 fmtVolume（成交量手），
  排行榜/资金流复用；openStockBySecid 通用打开（搜索/自选/排行榜/战绩共用）
- 安全性：新板块全部外部文本经 esc() 转义；注销账号同步清理 predict_log
- 验证：后端接口 curl 实测（leaderboard/fflow 真实数据）、预测结算端到端、
  I18N zh/en 240 key 完全一致、JS 语法检查、Edge headless CDP 渲染验证
  （排行榜 3 行/资金流 5 行+画布/统计 5 卡+14 列+直方图/战绩 2 行+详情 4 行）

## v0.21.1 (2026-08-13)
- 注册二次确认：注册表单新增"再次输入密码"输入框（仅注册 tab 显示）；
  输入时实时红字提示两次是否一致（不一致显示"两次输入的密码不一致"），
  提交前再次拦截（前端保证一致才发请求）；中英双语
- 图表悬浮窗/图例双语化（用户反馈：中英切换后悬浮窗仍显示"收盘/BOLL上中下"等中文）：
  - 悬浮窗字段：收盘 → tipClose、BOLL上/中/下 → bollUp/bollMid/bollLow
  - K线图图例：收盘价、涨/跌、上/中/下轨
  - 成交量图图例：阳线量/阴线量/单位：手
  - 涨跌幅图图例：上涨/下跌/单位：%
  - MACD 图例：柱(2×差值)、RSI 图例：30 超卖/70 超买
  - 拟合面板：真实收盘(现价/虚线) → 双语 key
  - 序列名（arrs/lines）同步双语
- 验证：ad-hoc 端到端 44 项全过（JS 逻辑区无中文硬编码残留、password2Error 一致/不一致/空
  三态、注册+登录实测、JS 语法、I18N zh/en key 一致）

## v0.21.0 (2026-08-13)
- 注销账号：功能页右上角"注销账号"按钮（红字）+ 弹窗；
  需密保邮箱 + 验证码 + 滑块三重确认；删除该用户全部数据（users/watchlist/ai_cache/history/sessions）
- 忘记密码：登录页新增"忘记密码？"链接 + 重置弹窗（账号 + 密保邮箱 + 新密码 + 验证码 + 滑块）；
  重置成功后该用户全部会话失效，用新密码登录
- 会话持久化：token 落盘 SQLite sessions 表，服务重启后仍保持登录（解决"关浏览器/重启后要重新登录"）；
  启动时清理过期会话；登出/改密/重置/注销同步删库
- K线日期校验：开始日期不能晚于结束日期、结束日期不能晚于今天（后端 400 + 友好提示）
- README 修正：API 表补 /api/delete-account、/api/reset-password 与登录邮箱说明；
  目录结构澄清 crawler/ examples/ 为历史遗留（主程序不依赖）
- 验证：ad-hoc 端到端 33 项全过（会话重启恢复、注销邮箱错/成功/数据删除、
  重置邮箱错/成功/旧会话失效/新密码登录、日期校验 400、前端元素、JS 语法、I18N key 一致）

## v0.20.3 (2026-08-13)
- 修改密码补全人机验证与实时校验（用户要求：改密要有非法提示、两次一致提示、验证码+滑块）：
  - 实时红字提示：新密码输入时即时校验格式（过短/过长/特殊字符/缺大写/缺小写/缺数字），
    确认密码输入时实时比对两次是否一致（不一致提示"两次输入的新密码不一致"）
  - 图形验证码：改密弹窗新增验证码输入框+图片（点击换一张），提交前必须通过
  - 滑块人机验证：改密弹窗弹出滑块拼图（遮罩变暗），拖动碎片到缺口完成验证；
    滑块逻辑从登录页移植到功能页（样式/拖动/位置+时长+轨迹三重校验一致）
  - 后端 /api/change-password 同步加验证码 + 滑块校验（缺验证码/验证码错/滑块失败均 400，
    验证码一次性），防机器人批量改密
- 验证：ad-hoc 端到端 24 项全过（缺验证码/验证码错/滑块失败/全通过/新密码登录/旧密码拒绝、
  JS 语法、弹窗元素、密码实时提示逻辑、I18N key 一致）

## v0.20.2 (2026-08-13)
- 密保邮箱体系（用户要求：改密不再需要原密码，改为密保邮箱验证；注册新增密保邮箱）：
  - users 表新增 email 列，老库启动时自动迁移（ALTER TABLE，幂等），空串表示未设置
  - 注册：密保邮箱必填 + 格式校验（正则：含 @、域名后缀 2+ 字母）+ 长度上限 100；
    邮箱统一转小写存储；用户名和邮箱一对一查重（唯一索引，冲突分别提示
    "用户名已存在" / "该邮箱已被使用"）
  - 修改密码：无需原密码，输入密保邮箱验证通过即可改密；
    未设置邮箱的账号提示无法修改；改密后其他会话照旧失效
  - 前端：注册表单新增邮箱输入框（仅注册 tab 显示）+ 实时红字格式校验；
    修改密码弹窗原密码框改为密保邮箱框；中英双语提示
- 市值单位双语：英文界面总市值用 B/M/K/T（1.50B / 23.00M / 4.50K），中文保持亿/万
- 验证：ad-hoc 端到端 24 项全过（老库迁移、邮箱格式/缺邮箱/用户名占用/邮箱占用/大小写归一化、
  改密邮箱错/成功/新密码登录/旧密码拒绝、fmtAmount 英文单位、I18N key 一致、JS 语法）

## v0.20.1 (2026-08-13)
- 数据库与代码彻底分离（服务器更新不再丢数据）：
  - deploy/start_server.bat：Windows 启动脚本，数据库放到项目目录外的
    C:\stock-app\data\stock_forecast.db（STOCK_DB 环境变量），更新代码永不覆盖数据
  - deploy/update_server.bat：Windows 一键更新（备份数据库 → 下载 GitHub ZIP →
    解压替换代码 → 提示重启），全程不碰 data 目录
  - 后端每次启动自动备份数据库到 backups/db/<时间戳>/（sqlite3 在线备份，
    保留最近 30 份），误操作也能找回
- 排查记录：服务器数据库曾因"整个目录复制覆盖"导致 users 表空、老账号登录失败；
  自选股数据可从浏览器 localStorage 经 v0.16.6 迁移逻辑恢复。部署文档（README）
  补充核心原则：数据库文件与代码目录分离，更新只替换代码文件。

## v0.20.0 (2026-08-13)
- 安全加固（公开部署前必备）：
  - 会话 token 7 天过期 + 过期自动清理（原永久有效，泄露即长期可用）
  - IP 限速：登录 10 次/分、注册 5 次/分、验证码/滑块 20 次/分；
    连续 5 次登录失败锁定该 IP 10 分钟（防爆破/防刷验证码）
  - 请求体上限 1MB（超出 413）；并发请求上限 50（超出 503，防线程无限增长）
  - 安全响应头：CSP / X-Frame-Options SAMEORIGIN / X-Content-Type-Options nosniff /
    Referrer-Policy no-referrer（防 XSS/点击劫持/MIME 嗅探）
  - XSS 转义：前端新增 esc() 函数，所有外部文本（API 返回/AI 解读/用户输入）拼入
    innerHTML 前转义（候选列表/自选股行/当前股标题/用户名/AI 解读/对比列表）
  - HTTPS 支持：STOCK_SSL_CERT / STOCK_SSL_KEY 环境变量直接启用 TLS；
    README 补充 Caddy 反代方案（自动证书更省心）
- 功能：修改密码（功能页右上角"修改密码"按钮 + 弹窗；旧密码验证 + 新密码格式校验 +
  改密后该用户其他会话全部失效，当前会话保留）
- 运维：
  - deploy/stock-forecast.service systemd 常驻单元（含 STOCK_HOST/PORT/SSL/日志注释）
  - tools/backup_db.sh 数据库备份脚本（sqlite3 在线备份，保留最近 30 份，可配 crontab）
  - 访问日志开关：STOCK_LOG=1 时打印请求日志（默认静默）
  - requirements.txt 精简为 requests（移除未用的 pandas/bs4/lxml）
- 体验：新用户引导条（登录后首次显示使用步骤：搜索→加自选→看K线，可关闭，localStorage 记忆）
- 文档：README 全面修正（删除已废弃的热力图描述、三表→四表、目录结构补 deploy/tools、
  API 表补 /api/slider 与 /api/change-password、新增安全说明与 HTTPS/备份部署章节）
- 验证：ad-hoc 端到端 35 项全过（会话过期清理、IP 限速、413/安全头、
  改密全链路（旧密码错/格式错/成功/旧会话踢出/新密码登录）、esc 转义、静态断言新文件存在）

## v0.19.1 (2026-08-13)
- 登录/注册滑块人机验证（用户要求：单独的窗口，弹出时周围变暗，防止机器人大量入侵）：
  - 点击登录/注册 → 先做格式校验 → 弹出滑块验证弹窗（半透明遮罩变暗 + 居中白卡）
  - 拼图：7x4 网格色块 SVG 背景（零依赖）+ 随机缺口 + 碎片，缺口纵向位置随响应下发
    （gap_y 供碎片对齐缺口行，缺口 x 保密仅后端持有）
  - 拖动交互：滑块按钮 mousedown → 碎片跟随移动 → 松手提交；拖动过程记录轨迹点数
  - 后端三重校验（/api/register 与 /api/login 共用 _verify_slider）：
    ① 碎片中心 x 与缺口中心 x 误差 ≤ 12px（对齐才通过）
    ② 拖动时长 ≥ 300ms（脚本瞬间位移被拒）
    ③ 轨迹点数 ≥ 3（纯 POST 无拖动过程被拒）
    ④ 一次性：通过即删除；2 分钟过期；无参数/伪造 id 直接拒绝
  - 前端自检 + 后端强校验双保险：拖动太假（<300ms 或 <3 点）前端直接提示重试，
    后端校验失败返回 400"滑块验证失败，请重试"并刷新滑块
  - 切换登录/注册 tab 关闭滑块弹窗；语言切换弹窗文案同步（中英）
  - 图形验证码保留：注册 = 滑块 + 字符验证码；登录 = 滑块（无需字符码）
- 验证：后端单元级 8 项（位置/时长/轨迹/一次性/过期/无参）+ 注册链路 2 项
  （带滑块成功、无滑块 400）+ CDP 登录链路 6 项（点登录弹遮罩、异步拖动
  模拟真人轨迹、登录成功跳转 /app、token 48 位写入）共 16 项全过

## v0.19.0 (2026-08-13)
- 登录页背景图：桌面"股市概念图.jpg"（3300x2100）压缩至 1920w q80 后 base64 内嵌 CSS
  （约 223KB，无需后端静态路由、不触发 GitHub 上传脚本 .jpg 排除规则），
  背景 cover 居中 + 半透明白色遮罩（backdrop-filter blur）保证卡片可读
- 登录窗口增大：380px → 440px，内边距同步加大
- 右上角 ENG/中文 切换按钮（左右选择式，仿南大 authserver 样式）：
  - 左 Eng 右 中文，选中项蓝色高亮，圆角胶囊分段控件，固定右上角
  - 登录页（position:fixed 右上角）+ 功能页（header 右上角 authArea 旁）各一个
- 中英双语切换（登录页 + 功能页）：
  - 语言偏好存 localStorage `sf_lang`，两页共享；登录页点击即时切换，
    功能页点击 location.reload() 重渲染（动态文案多，刷新最可靠）
  - I18N 字典 + t() 函数：静态 UI 用 data-i18n/data-i18n-ph 属性，JS 动态文案
    （状态提示/自选股表头/图表浮窗字段/AI 面板/对比弹窗等 60+ 项）用 t() 包裹
  - 覆盖范围：标题、查询行、tabs、图表类型下拉、AI/对比/分享按钮、自选股表头与状态列、
    setStatus 高频提示、图表浮窗字段（日期/开盘/最高/最低/成交量/涨跌幅/状态）、
    拟合/预测面板、AI 面板、对比弹窗、分享卡片提示
  - 模型名（ARIMA/ETS/Prophet/SVR/RF）保留英文，双语均友好
- 验证：node --check 双页 JS + Edge headless CDP 端到端（登录页背景含图、
  切换按钮存在、切英文标题/按钮/placeholder 全变、功能页登录后英文生效、
  切回中文生效；8 项断言全过）

## v0.18.1 (2026-08-13)
- 登录后自动获取自选股图表数据（用户要求：登录不仅自动获取实时行情，还要自动获取图表，无需再点击自选股）：
  - loadWatchFromCloud 登录成功 → 自动触发 prefetchWatchCharts() 串行预取全部自选股日K数据（写缓存，不打断当前视图）
  - 按自选股列表从上到下顺序逐只获取（串行 await），避免多只同时请求导致数据源卡顿/拦截
  - 自选股行右侧新增"图表"状态列：待获取（灰）→ 正在获取数据（橙）→ 已获取（绿）/ 获取失败（红），
    状态实时刷新；已缓存股票渲染时直接标记"已获取"
  - 点击正在预取的自选股：doKlineSecid 等待进行中的预取请求并复用结果，不重复下载（_watchChartPromise）
  - 预取成功后点击自选股命中缓存秒开；setChartData 时状态列同步为"已获取"
  - 删除自选股清状态；退出登录清空全部预取状态
- 验证：node --check JS 语法 + Edge headless CDP 端到端（登录注入 → 自动预取 → 串行确认
  "1.600519=正在获取数据 且 1.601088=待获取" → 完成后两只均"已获取"、2 条 kline 缓存写入、无 JS 错误）

## v0.18.0 (2026-08-13)
- 登录/注册输入实时合法性红字提示（用户要求：输入第一个字符起不合法就在输入框旁红字提示具体原因）：
  - 用户名旁 hint：过短/过长/含非法字符/不能只用下划线，具体原因逐条实时提示
  - 密码旁 hint：过短/过长/含非法字符/缺大写/缺小写/缺数字，具体原因逐条实时提示
  - 输入合法时提示自动消失（:empty 无占位）；切换登录/注册 tab 清空提示
  - 提交时（登录/注册）复用同一套校验函数，格式不合法不发请求（前后端双重校验不变）
- 新增规则：用户名不能只用下划线（纯 _ 串如 "___" 拒绝，前端 + 后端同步校验）
- 验证：py_compile 后端 + node --check 前端 JS 语法通过

## v0.17.2 (2026-08-12)
- 打开网站一律先显示登录页（用户要求：还会闪烁一次，直接改为先打开登录页）：
  - 删除登录页"已登录自动跳转 /app"逻辑——每次打开网站先落在登录页，
    用户手动登录/注册后进入功能页；彻底杜绝登录页⇄功能页闪烁
- 注册规则严格化（用户要求）：
  - 用户名：仅限字母/数字/下划线，长度 3-20（前后端双重校验）
  - 密码：仅限字母/数字（无特殊字符），长度 8-20，
    必须同时包含大写字母、小写字母和数字（前后端双重校验）
  - 输入不规范时前端即时提示具体原因（不发起请求）
  - 用户名重复 → 提示"用户名已存在，请换一个"
    （用户指定提示文案；同时修复注册异常分类——仅唯一约束冲突判定为
    用户名已存在，其他错误返回 500 不误导；补 backend.py 缺失的 sqlite3 import）
- 验证：ad-hoc 正则 19 项 + 真实 HTTP 注册流程 7 项全过
  （特殊字符/中文/过短用户名拒绝、无数字/特殊字符密码拒绝、
  合法注册成功、重复用户名冲突提示）+ py_compile/node --check

## v0.17.1 (2026-08-12)
- 修复登录页⇄功能页反复切换闪烁（用户反馈：有些电脑保存先前登录状态后闪烁）：
  - 根因：本地残留无效 token（如服务器重启后 session 内存清空）时——
    登录页检测到 token → 跳 /app；功能页 /api/me 返回 401 → 跳回 /；
    登录页又检测到 token → 再跳 /app → 无限循环闪烁
  - 另一诱因：功能页 cloudGet 网络错误返回 null 被误判为"未登录"跳转，
    与登录页的跳转互相触发
  - 修复：
    - 登录页：token 请求 /api/me 返回 401 时清掉本地 token 并留在登录页；
      网络错误不跳转不清 token
    - 功能页：cloudGet 网络错误返回 {__netError:true} 标记，
      loadWatchFromCloud 区分网络错误（提示重试，不跳转）
    - 401 跳转加 _authRedirecting 防抖标记，避免重复跳转
- 验证（Edge headless CDP 实测）：登录页残留无效 token → 自动清理并停留 /，
  3 秒后仍在 /；有效 token → 进入 /app 停留，3 秒后仍在 /app，无循环

## v0.17.0 (2026-08-12)
- 登录改为独立页面（用户要求：登录不要做成窗口，做成单独 html 页面）：
  - 新增 static/login.html：独立登录/注册页（卡片布局、登录/注册 tab、
    验证码、成功后跳转 /app）
  - 路由调整：/ → 登录/注册页；/app → 功能页（原 / 的内容）
  - 功能页 index.html：删除模态框 HTML/JS；未登录访问 /app 自动跳回 /；
    退出登录清空本地数据后返回登录页；登录失效（401）同样跳回 /
  - backup.sh FILES 清单新增 static/login.html
- 验证（Edge headless CDP 实测）：/ 显示登录卡片无模态框；未登录访问 /app
  自动跳回 /；有 token 访问 /app 正常显示功能页与用户名/退出按钮

## v0.16.10 (2026-08-12)
- 修复其他电脑上查询股票名显示成 sh601088（用户反馈兼容性问题）：
  - 根因：东财K线失败→新浪备用取名称时，_sina_stock_name 失败返回 symbol
    （内部代码 sh601088）当股票名；/api/kline 把它作为 data.name 返回，
    前端 setChartData 优先用 data.name（非空）覆盖了搜索候选的正确名称
  - 修复（三级兜底）：
    - _sina_stock_name 失败返回空串（绝不返回 sh601088 内部代码），
      且补设 gbk 编码、超时 10s→4s
    - /api/kline 新增 name 参数：接口名称为空或匹配内部代码格式
      （^[a-z]{2}\d{6}$）时用前端搜索候选名兜底
    - 前端 doKlineSecid 请求带 name；setChartData 双保险——
      data.name 是内部代码格式时用传入 name
- 验证（ad-hoc 9 项）：后端兜底规则三例（内部代码/空/真名）、
  新浪名称真实调用、失败返回空串、前端 name 参数与 goodName 规则、py_compile

## v0.16.9 (2026-08-12)
- 实时行情三重保护（用户要求：东财长期失败，新浪放第一位，再加备用源）：
  - 优先级重排：新浪批量（主）→ 腾讯批量（备1）→ 东财批量（备2）→ 逐只兜底
  - 新增 _tencent_quotes_batch：腾讯批量行情，含 PE/PB/总市值/流通市值
    （字段 39/46/45/44，市值亿→元），一次请求全部股票
  - _quote_tencent 从"仅基本面"扩展为完整单只行情（价格/涨跌幅/量额/基本面），
    修正涨跌幅负数被 num(v>0) 过滤的 bug
  - 单只 fetch_quote 同步重排：新浪 → 腾讯 → 东财
  - 东财批量 retries 降到 1、东财单股 retries 降到 1（快速失败尽早切源）
  - 效果：主链（新浪）3 只 0.61s；新浪挂→腾讯兜底秒回；
    三重全挂 4.5s 返回空（前端 30s 自动重试，不卡死）
- 验证（ad-hoc 10 项）：新浪主链/腾讯批量（负涨跌幅/PE/PB/市值）/腾讯单只/
  新浪挂→腾讯兜底/全挂不崩溃且限时/py_compile

## v0.16.8 (2026-08-12)
- 修复实时行情长时间加载不出来（用户反馈：登录后自选股实时行情经常获取不到）：
  - 根因：东财批量行情接口在云服务器 IP 段失败后，代码落到逐只串行回退——
    每只股票依次"东财 2 次重试 + 新浪 1 次"，每次超时 10 秒；
    N 只自选股最坏要几十秒到几分钟，前端一直转圈
  - 修复：
    - 行情类接口超时从 10s 降到 4s（get_json 新增 timeout 参数，快速失败尽早切备用源）
    - 新增新浪批量接口回退（_sina_quotes_batch：一次请求全部股票，
      替代逐只串行；实测 3 只 0.24s）
    - 批量失败后补齐基本面跳过东财单股（skip_eastmoney），直接腾讯补齐，
      不再每只等东财 2 次重试+退避
  - 效果：东财全挂场景 3 只股票从 13.78s 降到 4.37s（快 3 倍），正常场景秒回
  - 前端失败时保留旧数据 + 30s 自动重试（原有逻辑，未动）
- 验证（ad-hoc）：新浪批量解析正确/字段齐全、东财失败回退新浪批量、
  回退 3 只 4.37s <6s、skip_eastmoney 生效、py_compile

## v0.16.7 (2026-08-12)
- 打开网站必须纯净空白 + 弹登录（用户强烈要求"打开网站页面时候就是最纯粹的空白"）：
  - 根因：初始化时 _watch 无条件从 localStorage 读 wl，即使未登录，
    本地残留自选股仍会进入内存（虽然列表渲染有守卫，但不够彻底）
  - _watch 改为恒空数组，未登录绝不读本地 wl；登录后由 loadWatchFromCloud
    以云端为准加载，云端空时才读本地 wl 迁移到账号（不丢早期录入数据）
  - 本地 aiCache/候选列表/AI面板 均不影响初始渲染（面板默认隐藏、候选为空）
- 验证（Edge headless CDP 实测，预埋残留数据后重载）：残留股不显示、
  _watch=0、登录弹窗自动弹出、AI面板隐藏、候选为空、无 JS 错误

## v0.16.6 (2026-08-12)
- 删除当前查询股票标题右侧涨跌幅（用户多次反馈"显示不出来"）：
  - 根因：涨跌幅依赖自选股列表数据行（syncTitleFromWatch 投影），当前股不在
    自选股/未登录时永远显示 —，且易与自选股行产生不一致
  - 删除标题 .chg 元素及 syncTitleFromWatch 函数/调用，标题只显示 名称+代码
- 退出登录清空自选股（用户要求）：
  - 退出时清空 _watch/_watchQuotes 内存 + localStorage wl，页面自选股区
    回到"登录后查看自选股"提示
- 登录后显示云端自选股 + 本地旧数据迁移（用户要求："账号的意义"）：
  - 云端有数据 → 以云端为准显示该账号自选股
  - 云端空但本地有自选股（早期未登录时代录入）→ 自动上传迁移到当前账号云端，
    并提示"已将本机 N 只自选股同步到账号 xxx"
- 验证（Edge headless CDP 实测）：标题无 .chg 仅名称+代码；退出后 _watch 清空、
  localStorage wl 清除、自选股区显示登录提示

## v0.16.5 (2026-08-12)
- 修复未登录显示本地自选股缓存（用户反馈：本机打开网页未登录就看到之前自选股）：
  - 根因：_watch 从 localStorage 加载本地缓存，未登录也渲染；用户数据属于登录用户
  - renderWatchlist：未登录时不渲染，显示"登录后查看自选股"
  - refreshAllQuotes：未登录不请求实时行情（本地缓存可能非本人数据）
  - loadWatchFromCloud：登录后以云端数据为准（不合并本地旧缓存，防残留）
  - addWatch：未登录点击"＋"提示先登录并弹登录窗
- 验证（Edge headless CDP 实测）：未登录+本地有缓存 → 自选股区只显示登录提示
  无残留股；登录后（有 token）→ 正常渲染

## v0.16.4 (2026-08-12)
- 打开网站自动弹出登录窗口（用户要求）：
  - loadWatchFromCloud 校验登录态：未登录 → 300ms 后自动弹出登录/注册窗口
  - 已登录（token 有效）→ 不弹，正常显示用户名与云端自选股
- 顺带修复 v0.16.0 引入的隐藏 bug：loadWatchFromCloud 函数定义被误删
  （调用处仍在，运行时报 ReferenceError，自选股云端同步静默失效）
- 注：用户最初要求改标题/域名，已确认保留原标题、暂不配域名
- 验证（Edge headless CDP 实测）：未登录自动弹窗 modal=flex 且默认登录 tab、
  loadWatchFromCloud 函数存在、无 JS 错误

## v0.16.3 (2026-08-12)
- AI 解读缓存跨天自动刷新（用户要求：明天登录日期变化后 AI 分析也要更新，
  但不要重新生成数据产生冗余）：
  - K线/图表：缓存 key 含日期范围（kline_<secid>_day_<start>_<end>），
    明天登录日期变化自动不命中 → 重新下载重绘（原本就有，确认无需改动）
  - AI 解读：缓存从纯文本改为 {text, ts}，新增 aiCacheValid 判断——
    当天生成直接显示；跨天/旧格式自动重新解读并**覆盖同一条缓存**
    （前端 localStorage + 后端 INSERT OR REPLACE 都不新增记录，无冗余）
  - 自选股/实时行情为实时拉取，不受日期变化影响
  - 旧格式字符串缓存兼容（视为过期，重新解读一次后转为新格式）
- 验证：ad-hoc 11 项全过（当天有效/跨天失效/旧格式失效/空失效/
  后端覆盖单行不增长/覆盖后文本更新/py_compile/node --check/前端函数）

## v0.16.2 (2026-08-12)
- 搜索增加腾讯备用源（用户反馈：云服务器上搜索显示"未找到该股票"）：
  - 根因（第二次定位）：东财 searchapi 对云服务器 IP 段风控——接口 200 但响应里
    没有股票数据（只有 result.passportWeb 用户搜索结果，QuotationCodeTable 缺失），
    返回空候选列表 → 前端"未找到该股票"。v0.16.1 解决的 JSONP 解析是前提，
    但数据本身为空，解析成功也拿不到股票
  - 修复：新增 _tencent_search（腾讯 smartbox.gtimg.cn/s3 股票搜索接口），
    东财搜索失败（连接错误）或返回空股票结果时自动回退腾讯源
  - 腾讯响应解析坑：多个结果用 ^ 或 ; 分隔（实测中国神华返回 sh^hk^us 多条）；
    港股 hk/美股 us 也标 GP 类型必须按市场 sh/sz + 代码纯数字过滤；
    名称是 \u4e2d\u56fd 形式 unicode 转义需 decode("unicode_escape")
- 验证：ad-hoc 9 项全过（腾讯代码/中文搜索、secid 格式、名称解码、过滤
  港股美股、东财空壳回退、东财断连回退、字段完整、py_compile）

## v0.16.1 (2026-08-12)
- 修复云服务器上搜索失败（用户反馈：搜索提示"Expecting value: line 1 column 1"）：
  - 根因：东财 searchapi 对不同来源返回格式不同——本机直连返回纯 JSON，
    云服务器 IP 段返回 JSONP（jQuery3510...({...}) 回调包裹）。
    resp.json() 遇 JSONP 直接抛 ValueError → 4 次重试全败 → 搜索失败
  - 修复：get_json 改用 _parse_json_text——兼容纯 JSON 和 JSONP
    （剥掉 `函数名(...)` 外壳再 json.loads），其余接口（K线/行情）同步受益
- 验证：ad-hoc 8 项全过（纯JSON/JSONP对象/JSONP数组/空白/空响应/垃圾内容/
  get_json 端到端模拟服务器JSONP响应/py_compile）

## v0.16.0 (2026-08-12)
- 用户登录注册系统（用户要求：登录后使用自己的自选股等数据）：
  - 后端认证：users 表（PBKDF2-SHA256 加盐哈希，不存明文）、注册/登录/登出 API、
    Bearer token 会话（内存，重启需重新登录）、SVG 图形验证码（4 位字符+干扰线，
    零依赖，2 分钟有效一次性）
  - 用户数据隔离：watchlist/ai-cache/history 全部按登录 user_id 隔离，
    未登录访问返回 401（浏览行情/K线不受限，仅用户数据需登录）
  - 前端：header 登录/注册按钮 + 模态框（登录/注册 tab，注册含验证码图片，
    点图刷新）；token 存 localStorage；cloudPost/cloudGet 自动带
    Authorization 头；401 自动清除 token 提示重新登录；未登录加自选提示
    仅本机保存
  - 注册即登录；登出清除本地 token
- 验证：后端认证 15 项单测全过（未登录401/验证码错误/注册成功/重复用户名/
  登录成功/me/自选增删/用户隔离/登出失效/AI缓存+历史）；Edge headless CDP
  实测注册→登出→重登全流程（modal 关闭、token 落库、界面切换正确）；
  数据库确认 alice 用户哈希 64 位+盐独立

## v0.15.0 (2026-08-12)
- 云数据库支持（用户要求：项目上云、存储用户数据）：
  - 新增 db.py：SQLite（标准库零依赖）三张表 watchlist/ai_cache/history，
    数据库文件 stock_forecast.db（已 gitignore），环境变量 STOCK_DB 可改路径
  - 后端 API：GET/POST /api/watchlist（自选股增删查）、GET/POST /api/ai-cache
    （AI 解读缓存读写）、GET/POST /api/history（查询历史记录，保留最近200条）
  - 前端云端同步：加自选/删自选 → POST 云端；AI 解读本地无缓存先查云端、
    成功后写云端；打开股票记录历史；页面加载拉云端自选股合并本地
    （localStorage 保留做离线兜底，云端接口失败静默不阻塞）
  - main() 支持环境变量 STOCK_HOST/STOCK_PORT（云部署：STOCK_HOST=0.0.0.0
    STOCK_PORT=8000）；公网模式关闭"页面关闭自动停止"，需 Ctrl+C 停止
  - backup.sh 加 db.py；.gitignore 加数据库文件
- 验证：后端 API 9 项 curl 全过（增删查自选/AI缓存/历史）；Edge headless CDP
  前端→后端→SQLite 端到端写库实测通过；node --check/py_compile 通过

## v0.14.3 (2026-08-12)
- 移除分享后自动打开图片功能（用户反馈：触发弹窗拦截、打开空白 blank 页，影响使用）：
  - 删除 window.open(dataUrl) 自动打开逻辑及拦截提示分支
  - 恢复为纯下载：导出 PNG → a.download 触发下载 → 状态提示
    "卡片已导出为 PNG 图片文件"
- 验证：ad-hoc 断言 10 项全过（无 window.open 残留、下载链路完整、
  node --check 语法通过）

## v0.14.2 (2026-08-12)
- 两项修复（用户反馈）：
  1. 鼠标不在图表范围内时不显示十字虚线（分享截图会带上）：
     根因 cv.onmouseleave 只隐藏浮窗不重绘，十字线残留画布。
     修复：mouseleave 时按当前视图重绘（fit → drawFit，其他 → paint），
     截图不再带虚线
  2. 分享卡片下载后自动打开图片：下载触发后 window.open(dataUrl)
     自动打开图片预览；headless/被弹窗拦截时捕获 null 给提示
     （"浏览器拦截自动打开，请查看下载"），不影响下载本身
- 验证（Edge headless CDP 实测）：悬停画布哈希 244411693 → mouseleave 后
  1174951197（重绘清除十字线）；分享后 window.open 调用计数 1、无 JS 错误

## v0.14.1 (2026-08-12)
- 分享卡片导出高清修复（用户反馈：截图后像素低看不清）：
  - 根因：导出画布固定 560px 逻辑宽度，PNG 输出仅 560px 宽，放大即模糊
  - 修复：画布物理像素放大 2 倍（c.width=W*2 + ctx.scale(2,2)），
    逻辑坐标不变整体放大绘制——文字/表格/边框全高清，图表截图源
    （主画布 1140px）不变
- 验证（Edge headless CDP 实测）：导出画布物理尺寸 1120×1230
  （原 560×615，清晰度翻倍）、状态提示成功、无 JS 错误

## v0.14.0 (2026-08-12)
- 分享卡片改为导出 PNG 图片文件（用户反馈：原方案弹空白新窗口，无法分享给他人）：
  - 移除 window.open + document.write 空白页方案
  - 新方案：离屏 canvas 绘制整张卡片（背景/白卡圆角/标题+代码/日期范围副标题/
    图表嵌入/六行指标表格/页脚免责声明）→ toDataURL → a.download 触发下载，
    文件名 行情卡片_<股票名>_<末日>.png
  - 内容与原卡片一致：最新收盘/涨跌幅/MA5/MA20/RSI(14)/DIF DEA 六项 + 图表截图
- 验证（Edge headless CDP 实测）：注入模拟数据 → 点分享按钮 → 状态提示
  "卡片已导出为 PNG 图片文件"、无 JS 错误

## v0.13.9 (2026-08-12)
- AI 解读面板增加 ✕ 关闭按钮（用户要求）：标题行右侧新增关闭按钮，
  点击关闭面板（_aiPanelOpen 置 false + display:none），缓存保留，
  重新打开立即显示缓存
- 更换 DeepSeek API key（llm_key.txt，已 gitignore 不随仓库上传）；
  验证：API 直调 HTTP 200、finish=stop、模型正常回复
- 验证：JS node --check 语法通过

## v0.13.8 (2026-08-12)
- 修复自动打开浏览器失败问题（用户反馈"终端显示自动打开浏览器失败需要手动打开"）：
  - 根因：WSL interop 注册丢失（binfmt_misc 无 WSLInterop 条目）→ 所有 Windows
    程序（explorer.exe）从 WSL 执行报 Exec format error
  - open_browser 加固：win32 走 os.startfile；WSL 走 explorer.exe；按 errno 8
    分支明确提示 WSL interop 失效 + 给出 sudo 修复命令（不再是笼统的"请手动访问"）
- backup.sh 备份清单更新：补 app.py/backend.py/static/index.html/tests/test_backend.py
  /UPDATES.md，移除已删除的 crawler_stock.py
- 验证：py_compile 通过；ad-hoc 脚本实测 open_browser 失败路径输出正确
  （真实坏环境验证，非 mock）

## v0.13.7 (2026-08-11)
- AI 解读 token 增加（用户要求，减小被截断概率）：max_tokens 600 → 1200
  （finish_reason=="length" 截断重试机制保留，双保险）
- 对比图浮窗偶发重叠彻底修复：
  - 原避让只翻转一次且不验证结果，锁定浮窗宽时翻转后仍可能重叠
  - 重构为多级回退：候选位置（翻左/上方/下方）逐个用 overlap 二次检查，
    全部不通过则强制放到锁定浮窗正下方（必不重叠），再 clamp 画布内
- 验证：静态 8 项断言全过（含 node --check、py_compile）

## v0.13.6 (2026-08-11)
- 对比图点击图表外清除锁定（用户要求，与主图"点击图表外清除"一致）：
  - compareOverlay click 委托：点击 overlay 背景/控件区/画布容器（除
    图表本身与锁定浮窗）→ 清除 _cmpPin、隐藏两个浮窗、重绘
  - 画布（双击锁定/解锁）与锁定浮窗（拖动）不触发清除；
    拖动浮窗松手不误清
- 验证：静态 8 项断言全过（含 node --check）；Edge headless 环境故障
  持续，CDP 行为实测未跑（逻辑为简单 click 委托，代码路径已核对）

## v0.13.5 (2026-08-11)
- 对比图与指标图四项修复（用户反馈）：
  1. 鼠标移出坐标轴窗口抖动：浮窗定位 clamp 在画布内（不超出画布，
     避免撑出滚动条导致布局抖动）；结合 v0.13.3 的尺寸缓存彻底消除
  2. 锁定后虚线跟随：锁定纵线（蓝色实线）保持，鼠标移动额外画虚线
     纵线跟随 + 跟随浮窗（与主图"锁定线+十字线"一致）
  3. BOLL/RSI 最左侧未与坐标轴对齐：根因是指标预热期（BOLL 前 19 天、
     RSI 前 14 天数据为 null）导致数据从中间开始画。修复：新增
     firstValidIdx/indXOf——指标图从第一个有效数据点开始填满绘图区，
     x 轴日期跳过预热期；MACD/KDJ/MA5 同步处理；十字线/锁定线/日期
     标签统一适配（K线/VOL/涨跌幅图不受影响）
  4. RSI 图无跟随线窗口数值：根因是 tipContentFor 的 rsi 分支
     `const v = x != null ? ...` 少箭头函数，x 未定义抛 ReferenceError，
     整个 hover handler 中断。修复为箭头函数 + 传 D.rsi 值
- 验证：静态 19 项断言全过（含 node --check 语法、py_compile、pytest 回归）；
  行为验证受限于 Edge headless 当日环境故障（WSL 下启动即退出），
  CDP 实测未跑完，修复点均经代码路径核对

## v0.13.4 (2026-08-11)
- K线周期零延迟切换 + 对比图固定日K（用户要求）：
  - 方案选择：图类型大类别拆分（"日K线图/周K线图/月K线图"）+ 前端聚合
    预计算——切换零等待，不需要网络请求
  - 图类型下拉：kline 拆为 日K线图(kline)/周K线图(kline-week)/月K线图
    (kline-month)；删除独立 chartPeriod 下拉
  - switchPeriodData：周期缓存优先；无缓存用日K缓存 aggToPeriod 前端聚合
    （毫秒级）；状态明确提示"（前端聚合，零等待）"
  - 指标图（MA5/BOLL/VOL/涨跌幅/MACD/KDJ/RSI）统一切回日K数据——
    周期聚合后指标为空的问题一并解决
  - 多股对比固定用日K：cmpDataOf 只读日K缓存，不再取当前 D（主图在周K/
    月K时对比图仍显示日K）
  - aiCacheKey 按当前 K线周期映射（日K/周K/月K 解读分开缓存）
- 验证（Edge CDP）：
  - 日K 40 → 周K 6（"前端聚合，零等待"）→ 月K 2 → MACD 回 40（日K）
    → 回日K 40，全程无网络等待
  - 主图周K（6 根）时打开对比：对比图仍 40 天日K（06-01~07-10）
  - 无 JS 错误；静态 14 项断言全过

## v0.13.3 (2026-08-11)
- 对比图五项修复（用户反馈）：
  1. 鼠标出入窗口大小抖动：根因是 mousemove 重绘每次都重设 canvas 尺寸，
     滚动条出现/消失导致 parent 宽度变化 → 抖动。修复：_cmpSize 首次确定后
     固定，重绘不重设尺寸；重新打开 overlay 时重置
  2. 横轴日期显示不完整：只显示首尾两个日期 → 改为均匀 5 个日期刻度
  3. 锁定浮窗不能拖动：根因是 .cmp-tip 有 pointer-events:none，浮窗收不到
     鼠标事件。修复：#cmpTip 覆盖 pointer-events:auto + cursor:move
  4. 两个浮窗碰撞避让：跟随浮窗与锁定浮窗重叠时先翻转到锁定浮窗左侧，
     不行再上下错开（与主图 placeHoverTip 思路一致）
  5. 日K/周K/月K 切换不生效：
     - 状态提示明确周期（"中国神华 周K 数据已加载"）
     - 新增前端 aggToPeriod：周期请求失败时用日K缓存前端聚合兜底，
       保证切换必然生效
- 验证（Edge CDP）：
  - 锁定后拖动浮窗 +50px 精确跟随；拖动后 canvas 尺寸不变（无抖动）
  - 跟随浮窗 (550-722) 与锁定浮窗 (345-517) 不相交（避让生效）
  - 切周K：40 天日K → 6 根周K（首日 06-07、末日 07-10），状态提示正确
  - 无 JS 错误；静态 13 项断言全过

## v0.13.2 (2026-08-11)
- 对比图锁定后跟随浮窗恢复（用户反馈：双击锁定后移动鼠标没有浮窗跟随）：
  - 根因：锁定状态 mousemove 只画锁定纵线+锁定浮窗，跟随浮窗被吞掉
  - 修复：新增 cmpFollowTip 跟随浮窗，与锁定浮窗 cmpTip 并存——与主图
    （pinTip 锁定 + chartTip 跟随）交互一致：锁定后鼠标移动仍显示鼠标
    位置各股票数值，锁定纵线+锁定浮窗保持原位
  - 解锁/移出时跟随浮窗一并隐藏
- 日K/周K/月K 周期选择仅在 K 线图模式显示（用户反馈：切到其他图表
  周期选择应消失）：
  - chartType change 时按 currentType 显示/隐藏 chartPeriod（kline 显示，
    MACD/KDJ/RSI/MA5/BOLL/vol/change 隐藏）
- 验证（Edge CDP）：
  - 双击锁定后移动鼠标：锁定浮窗保持 (295,453)、跟随浮窗显示鼠标位置数据
    （2026-07-01 中国神华 127.83 (+27.83%)…）
  - 周期控制：初始显示 → 切 MACD 隐藏 → 切回 kline 显示 → 切 RSI 隐藏
  - 无 JS 错误；静态 12 项断言全过

## v0.13.1 (2026-08-11)
- 对比图双击锁定 + 浮窗拖动（用户要求，与其他图表一致）：
  - 双击锁定纵线（蓝色加粗）+ 小浮窗固定；锁定后鼠标移动浮窗不跟随
  - 锁定后浮窗可拖动（mousedown 拖动，同主图 pinTip）；再双击解锁
  - 移出画布时锁定纵线持续显示；关闭 overlay 自动清锁定
  - 重构 showCmpTipAt 统一浮窗填充/定位
- 实时行情基本面字段保险机制（用户反馈：一开始缺 PE/PB/市值后面一直拿不到）：
  - 根因：补齐逻辑只在"东财批量失败回退新浪"路径执行；东财批量成功但
    个别字段缺失（数据源偶发缺字段）时直接 return，永不补齐
  - 修复：提取 _fill_fundamentals（东财单股→腾讯双备源补齐），批量成功
    路径与回退路径都执行——任何一次刷新只要字段缺失都会尝试补齐
- 验证（Edge CDP + 后端实测）：
  - 双击锁定 xi=14、浮窗 (295,144)；鼠标移动后位置不变（不跟随）
  - 拖动浮窗 +40/+25 → 精确跟随；再双击解锁 + 浮窗隐藏
  - 构造全 None 字段行情 → _fill_fundamentals 补齐 pe18.8/pb2.03/mktcap
    7371.49 亿（东财失败自动走腾讯）
  - 无 JS 错误；静态 16 项断言全过

## v0.13.0 (2026-08-11)
- 总市值/流通市值补齐（用户反馈：PE/PB 修了市值没修）：
  - _quote_tencent 增加市值字段（腾讯 44=总市值亿、45=流通市值亿，转元）
  - fetch_quotes 新浪回退补齐逻辑扩展到 pe/pb/mktcap/float_mktcap 四字段
  - 实测：神华 mktcap 7371.49 亿（与真实值吻合）
- 多股对比图增强（用户要求）：
  - 日期范围选择（cmpFrom/cmpTo）：默认最近 30 天，不超各股票数据并集范围，
    显示"可用范围"提示；范围变更自动重绘；归一化改为以所选范围首日 = 100
  - 鼠标纵线 + 小号浮动窗口（cmp-tip，12px 小字号）：随鼠标显示各股票
    对应日期的归一化值与累计涨跌幅（红涨绿跌）；mouseleave 隐藏
  - 对比图不做放大模式（保持简单，与其他图表交互一致：纵线+浮窗）
- 验证（Edge CDP）：
  - 打开对比：默认范围 06-10~07-10（最近 30 天）+ 可用范围提示
  - 生成图后 mousemove：纵线 + 浮窗显示"2026-06-27 中国神华 127.31 (+27.31%)
    贵州茅台 127.31 (+27.31%)"
  - 改范围 06-20 起 → 重绘 len 31→21、首日 06-20；mouseleave 浮窗隐藏
  - 后端腾讯接口市值实测；无 JS 错误；静态 15 项断言全过

## v0.12.9 (2026-08-11)
- AI 解读窗口宽度与图表对齐（用户反馈"凸出来一点"）：
  - 根因：页面无 box-sizing:border-box，.wrap(padding 12px) 实际宽 1204px、
    .ai-panel(padding 16px) 实际宽 1212px——AI 窗口比图表凸出 8px
  - 修复：全局 `* { box-sizing: border-box; }`，所有 max-width 含 padding，
    各卡片完全对齐（CDP 实测 diff=0）
- PE/PB 实时数据读不出（用户反馈）：
  - 根因：东财在 WSL 下频繁断连（批量+单股全失败）→ 新浪回退无 PE/PB →
    前端显示"—"
  - 修复：新增腾讯行情接口备源 _quote_tencent（qt.gtimg.cn，字段 39=PE、
    46=PB，实测神华 18.8/2.03 合理）；新浪回退后 PE/PB 依次用东财单股、
    腾讯接口补齐（双备源）
- 验证：
  - 后端实测腾讯接口返回 {'pe': 18.8, 'pb': 2.03}
  - /api/quotes 全链路：东财失败→新浪回退→腾讯补齐，
    神华 PE18.8/PB2.03、茅台 PE20.35/PB7.23 全部有值
  - CDP：AI 面板 691px = 图表容器 691px（diff=0）
  - 无 JS 错误；静态 9 项断言全过

## v0.12.8 (2026-08-11)
- 删除热力图功能（用户反馈：仅涨跌幅矩形块、丢失 PE/PB/昨收等数据，无实际价值）：
  - 移除 _wlMode 状态、wlModeBtn 切换按钮、heat-grid/heat-cell 渲染分支、
    相关 CSS 及 click handler 分支；自选股恢复纯列表展示（数据完整）
- 刷新按钮人性化（用户反馈：点击无反馈 + 固定"10s"文案）：
  - 点击后立即提示"正在刷新行情..."（状态栏）
  - 按钮显示逐秒倒计时（10s→9s→…→0s），归零自动恢复"🔄 刷新"
  - 刷新成功提示"行情已更新 HH:MM:SS"（时间戳同步更新）
- 验证（Edge CDP）：
  - 热力图元素全部移除（无 heat-grid/wl-mode/wlModeBtn），列表行正常
  - 点击刷新：状态"正在刷新行情..."、按钮 10s 禁用 → 2s 后 8s → 12s 后恢复
  - 无 JS 错误；静态 14 项断言全过

## v0.12.7 (2026-08-11)
- 自选股实时行情手动刷新按钮 + PE/PB 刷新率优化（用户要求）：
  - 前端：自选股标题行新增"🔄 刷新"按钮，点击立即刷新行情；
    10s 节流——点击后按钮禁用并显示"刷新(10s)"，10s 后自动恢复
    （disabled 按钮天然拦截二次点击，防连续请求过快）；30s 自动刷新不变
  - 时间戳 span 独立（id=wlTs），刷新成功后只更新文字不重建按钮
  - 后端 PE/PB 刷新不出根因：东财批量接口失败时整体回退新浪，
    而新浪接口无 PE/PB 字段 → 基本面信息缺失
  - 修复：
    1. 东财批量接口重试 2 → 3 次，提高基本面命中率
    2. 新浪回退后对 PE/PB 缺失的股票，用东财单股接口补齐
       （_quote_eastmoney，字段 f162/f167）
- 验证（Edge CDP）：
  - 按钮存在（🔄 刷新）；点击后禁用 + "刷新(10s)"；10s 后自动恢复
  - 测试环境东财不可达 → 日志确认 3 次重试 → 新浪回退 → PE/PB 补齐路径执行
  - 30s 自动刷新定时器保留；热力图/模式切换正常
  - 无 JS 错误；静态 13 项断言全过

## v0.12.6 (2026-08-11)
- 修复 AI 解读断句问题（用户报告）：
  - 根因：DeepSeek 输出超过 max_tokens(400) 时被截断成半句，缓存把断句
    也存了，重开无法补充
  - 修复：
    1. 后端 max_tokens 400 → 600，减少截断概率
    2. 后端检查 finish_reason：=="length"（被截断）判定失败走重试，
       拿完整结果
    3. 前端 AI 面板新增"🔄 重新生成"按钮（aiTitleHtml 统一标题）：
       强制绕过缓存重新请求并覆盖缓存——即使已缓存断句也可重新生成
    4. requestAiInsight 增加 force 参数；面板事件委托监听 .ai-regen
- 验证（Edge CDP + 真实 DeepSeek 调用）：
  - 首次点击：面板显示"🔄 重新生成"按钮 + 新等待文案
  - 解读成功：198 字符完整文本（无断句）写入缓存
  - 点重新生成：立即显示"正在进行"（绕过缓存），新解读覆盖缓存
    （198 → 200 字符），文本结尾完整句
  - 无 JS 错误；静态 12 项断言全过

## v0.12.5 (2026-08-11)
- AI 解读提高成功率 + 等待文案优化（用户要求）：
  - 后端 ai_insight 增加 3 次重试（指数退避 2s/3s，timeout 20s）——
    网络波动/5xx 自动重试，不再一次失败就报错
  - 发现并修复"空内容"根因：DeepSeek 偶发返回空 content，此前会写入
    空缓存导致面板只剩标题、看似失效——后端空内容判定为失败走重试，
    前端对空结果防御（不写空缓存，提示重试）
  - 等待文案改为"正在进行AI技术解读，大约需要5-10s…"
  - 前端 fetch 加 60s 超时（AbortController）+ finally 清理定时器
- 验证（Edge CDP + 真实 DeepSeek 调用）：
  - 点击后立即显示新等待文案
  - 15 秒后返回完整解读（143 字符）并写入非空缓存
  - curl 后端直测 3.6s 成功（重试路径不破坏正常请求）
  - 无 JS 错误；静态 12 项断言全过

## v0.12.4 (2026-08-11)
- 修复 AI 解读"失效"问题（用户报告）：
  - 根因：窗口开着时切到无缓存的新股票，显示占位提示；此时再点 AI 按钮
    走开关逻辑反而把窗口关闭——看起来像解读失效
  - 修复：
    1. 切换股票无缓存时自动直接解读（不再占位询问），窗口保持打开
    2. 按钮开关修正：窗口开着且当前股票已有解读才关闭；无缓存时点击 =
       打开并触发解读
    3. 请求抽取为 requestAiInsight()，新增 _aiReqToken 请求序号——
       连续切换股票时旧股票的响应被丢弃，不覆盖新股票面板（防串号）
- 验证（Edge CDP + 真实 DeepSeek 调用）：
  - 神华首次点击 → 打开并请求；切茅台 → 自动请求（无需点击）→ 茅台解读
    返回并写缓存；神华旧响应被串号丢弃（防串号生效）
  - 切回神华 → 自动重新请求并写缓存；再切回茅台 → 立即显示缓存（无请求）
  - 开关：有缓存时点按钮关闭、再点立即显示缓存
  - 无 JS 错误；静态 12 项断言全过

## v0.12.3 (2026-08-11)
- AI 解读改为开关式交互（用户要求）：
  - 点击 ✨ AI 解读：窗口关闭时打开（有缓存立即显示，无缓存才请求并保存）；
    窗口打开时再点即关闭，缓存内容保留
  - 切换股票：窗口开着则保持打开，仅内容切换（有缓存显示对应解读，
    无缓存显示占位提示）；窗口关着则保持隐藏，切回缓存股票后点开立即显示
  - 新增 _aiPanelOpen 开关状态；解读缓存 _aiCache（localStorage 持久化）不变
- 验证（Edge CDP + 真实 DeepSeek 调用）：
  - 初始隐藏 → 首次点击打开并请求 → 真实解读写入缓存
  - 切换茅台 → 窗口保持打开、内容变占位提示；再点 → 窗口关闭（缓存保留）
  - 切回神华 → 仍关闭；再点 → 立即显示神华缓存（无重新请求）
  - 无 JS 错误；静态 12 项断言全过

## v0.12.2 (2026-08-11)
- AI 解读按股票缓存 + 切换股票同步（用户要求）：
  - 新增 _aiCache（localStorage 持久化，key=secid_周期）：解读成功后保存，
    再次查看直接显示不重新请求
  - aiBtn 点击：缓存命中直接显示；未命中才调 /api/insight 并写缓存
  - setChartData 切换股票时同步面板：新股票有缓存则显示对应解读，
    无缓存则隐藏（避免残留上一只股票的解读）；切回原股票秒显缓存
- 验证（Edge CDP + 真实 DeepSeek 调用）：
  - 打开有缓存股票 → 面板直接显示缓存解读（无请求）
  - 切茅台 → 面板隐藏；切回神华 → 面板显示缓存
  - 茅台首次点击 → 真实请求返回文本并写入 _aiCache + localStorage
  - 再次点击 → 立即显示缓存（无"正在请求"，不重复请求）
  - 无 JS 错误；静态 12 项断言全过

## v0.12.1 (2026-08-11)
- AI 解读接入 deepseek-v4-flash（用户指定模型配置）：
  - backend.py 新增 LLM_MODEL="deepseek-v4-flash"、LLM_BASE_URL="https://api.deepseek.com/v1"
    （OpenAI 兼容格式，可在常量处调整）
  - 请求地址改为 {base_url}/chat/completions，model 用 LLM_MODEL
  - API Key 写入项目 llm_key.txt，.gitignore 增加忽略——公开仓库不泄露
- 验证（真实调用）：
  - 20 日 mock 行情 → /api/insight → 返回完整中文解读
    （上升趋势/压力位 47.6/支撑 45.0/双顶风险提示），模型 deepseek-v4-flash

## v0.12.0 (2026-08-11) 大版本：代码拆分 + 指标三件套 + 周期切换 + AI 解读 + 多股对比 + 分享卡片 + 热力图

### 一、代码结构优化（用户要求）
- **拆分单文件**：crawler_stock.py(2373行) → `app.py`（启动入口）+ `backend.py`（数据/指标/模型/API/AI）+ `static/index.html`（前端）
  - 运行方式变更：`python3 app.py`（自动读取 static/index.html）
  - 模板改为启动时读文件并缓存（load_index_html），旧单文件已删除（备份保留）
- **pytest 单元测试**：`tests/test_backend.py` 8 项（MA/BOLL/EMA/MACD/KDJ/RSI/周期聚合/数据结构），`python3 -m pytest tests/ -q` 全过
- **README 重写**：功能清单、运行方式、技术架构（数据层/指标层/模型层/AI 层）、目录结构、API 表

### 二、功能增强
- **MACD/KDJ/RSI 指标**（图类型下拉新增 3 项，后端手写计算）：
  - MACD：EMA12-EMA26=DIF，EMA9(DIF)=DEA，柱=2×差值
  - KDJ：RSV→K(2/3递推)→D→J=3K-2D；RSI：Wilder 平滑，30/70 参考线
  - 放大模式 y 范围/悬停浮窗同步适配；旧缓存缺字段时友好提示重查
- **日K/周K/月K 周期切换**（chartPeriod 下拉）：
  - 东财原生周期接口（klt 101/102/103）；新浪回退时用 agg_period 日K聚合
  - 缓存 key 含周期；切换时自动重载当前股票
- **行情更新时间戳**：自选股标题行显示"更新于 HH:MM:SS"（每次批量刷新成功更新）

### 三、新功能
- **✨ AI 技术解读**：前端按钮 → /api/insight → DeepSeek 对最近 20 日行情生成 3-5 句中文解读（趋势/支撑压力/风险）；Key 读环境变量 DEEPSEEK_API_KEY 或项目目录 llm_key.txt，未配置时友好提示
- **⇄ 多股归一化对比**：当前股+自选股勾选 2+ 支 → 以各自首日收盘=100 画对比曲线（末端标注累计涨跌幅），一眼看相对强弱
- **📋 分享卡片**：生成含 K 线截图（canvas.toDataURL）+ 关键指标（收盘/涨跌幅/MA/RSI/MACD）的 HTML 卡片，新窗口展示可打印/另存
- **▦ 自选股热力图**：列表/热力图切换，色块按涨跌幅着色（红涨绿跌，深度随幅度），点击色块打开股票

### 验证
- 静态断言 + 前端 JS node --check + pytest 8/8
- Edge CDP 实测（mock 40 天数据）：
  - 8 种图类型切换绘制正常（MACD 61999 像素、图例正确）；悬停浮窗正常
  - 周期切 week 请求成功并缓存；时间戳随刷新更新
  - AI 解读无 key 显示配置提示（全链路通）
  - 多股对比：2 候选 → 归一化曲线绘制 13269 像素 → 关闭正常
  - 分享卡片点击不崩（headless 弹窗被拦时友好提示）
  - 热力图：2 色块（红 0.42 alpha 匹配幅度）、列表/网格切换正常
  - 无 JS 错误

### 兼容性说明
- 旧单文件 crawler_stock.py 已删除；运行改为 `python3 app.py`
- AI 解读需 DeepSeek Key（可选，不影响其他功能）

## v0.11.1 (2026-08-11)
- hover 浮动窗口与固定窗口(pinTip)碰撞避让（用户要求）：
  - 新增 placeHoverTip 统一定位：优先鼠标右侧、屏幕越界翻转、与 pinTip
    重叠时避让——先尝试翻转水平侧（滞回保持当前侧 _tipSide，避免左右
    抽搐），两侧都不可用时上下避让（优先 pinTip 上方）
  - 替换全部 4 处定位：cv 行情/拟合浮窗、fcv 未来预测浮窗、放大模式浮窗
- 验证（Edge CDP，40 天模拟数据）：
  - 单击锁定后 pinTip rect {242,488,384,593}；鼠标移到其右侧重叠区 →
    hover 浮窗避让到上方 {295,541,169,378}，与 pinTip 不相交
  - 鼠标微动后浮窗位置稳定（不左右抽搐，sideStable true）
  - 无 JS 错误；静态 15 项断言全过

## v0.11.0 (2026-08-11)
- 未录入股票时图表光标恢复正常（用户要求）：
  - 根因：CSS `canvas { cursor: crosshair }` 全局生效，无数据时也是十字形
  - 修复：drawTooltip/drawFutureTooltip 初始化 cv/fcv 为 default；无数据
    （含数据边界外）mousemove 保持 default，有数据时改 crosshair
- 放大拖拽经过浮动窗口不再卡顿（用户报告）：
  - 根因：拖拽 mousemove 监听在 zoomCanvas 上，鼠标移到 pinTip（上层
    元素）后事件不再到达 zoomCanvas，拖拽中断
  - 修复：拖拽逻辑改到 window 级 mousemove（事件冒泡必然到达），
    鼠标经过浮窗等任何上层元素时拖拽不断开；zoomCanvas 的 mousemove
    只保留 hover 十字线
- 验证（Edge CDP，40 天模拟数据）：
  - 无数据：初始 cursor default、mousemove 后仍 default
  - 有数据：mousemove → crosshair
  - 放大锁定后拖拽：mousemove 在 zoomCanvas 上 ZOOM {10,39}→{9,38}；
    mousemove dispatch 到 pinTip 上 → 继续 {4,33}（拖拽不断开）
  - 无 JS 错误；静态 15 项断言全过

## v0.10.10 (2026-08-11)
- 修复放大模式拖拽松手在固定窗口上被误判清除（用户报告 bug）：
  - 根因：拖拽 mousedown 在 zoomCanvas、鼠标松开在 pinTip 上时，浏览器
    的 click 事件在两者共同祖先（body）触发，target 非 canvas 也非
    .tip-box → 被 document"点击图表外清除"逻辑误判，pin 和窗口被清掉
  - 修复：新增 _zoomDragEnded——mouseup 时（拖拽结束）置标记，
    document click handler 消费该标记跳过清除；普通点击图表外清除不受影响
- 验证（Edge CDP，40 天模拟数据）：
  - 放大模式双击锁定 {view:chart, i:10} + 窗口显示
  - 拖拽（mousedown+mousemove）→ mouseup 在 pinTip 上 → dispatch body click
    → pin 保持 i:10、窗口仍显示、标记已消费
  - 无 JS 错误；静态 12 项断言全过

## v0.10.9 (2026-08-11)
- 放大模式锁定改为双击（用户要求，消除单击/拖拽代码冲突）：
  - 删除放大画布 click 锁定与 _zoomSuppressClick 拖拽抑制机制（不再需要），
    新增 zc dblclick 锁定参考线 + 固定窗口
  - 放大模式下单击不再产生任何锁定动作；拖拽只平移，双击才锁定
- 普通模式单击/双击判断（用户要求，避免同时触发）：
  - cv/fcv 单击锁定改为 250ms 延时执行（_clickTimer）；双击到达时
    clearTimeout 取消挂起的单击锁定并进入放大——双击不会先锁定再放大
  - cv.onclick/fcv.onclick 属性赋值改为 addEventListener
- 验证（Edge CDP，40 天模拟数据）：
  - 普通模式单击 → 立即读 pin null → 250ms 后 {view:chart, i:10} 锁定
  - 普通模式双击（0.7 宽处）→ 进入放大（overlay flex），pin 保持 i:10
    （双击未锁定新点，1 秒后仍不变）
  - 放大模式双击 zoomCanvas → pin {i:14} + 固定窗口显示
  - 放大模式单击 → pin 不变（不锁定）
  - 无 JS 错误；静态 18 项断言全过

## v0.10.8 (2026-08-11)
- 固定窗口出可见范围不再消失，停在边界（用户要求）：
  - showPinTip 删除"出范围隐藏"逻辑，改为把 x clamp 到绘图区左右边界
    （chart/fit: PAD.L~W-PAD.R；future: 70~W-24），窗口停在边界显示
- 手动拖动固定窗口后相对图表固定（用户要求）：
  - 新增 _pinTipManual：pinTip 拖动时记录相对图表的偏移 {dx,dy}
  - 放大模式下 showPinTip 若存在手动偏移 → 窗口按"图表位置+偏移"定位，
    不再吸附固定点、不随内容跳回；拖拽图表时窗口相对画布保持不动
  - 重新锁定（点击图表）、点击外部清除、进出放大时清除手动偏移
- 验证（Edge CDP，40 天模拟数据）：
  - pin.i=19，缩放到 {30,39}（固定点出范围）→ 窗口仍显示且停在左边界
    （boundAtLeft true）
  - 手动拖动窗口后拖拽图表 → 窗口位置不变（相对画布固定，
    fixedRelativeToChart true）
  - 重新点击锁定 → _pinTipManual 清除、新 pin {i:22} 生效
  - 无 JS 错误；静态 17 项断言全过

## v0.10.7 (2026-08-11)
- 修复放大后坐标轴消失（用户报告，clip 把轴标签也裁掉了）：
  - 根因：drawZoomCanvas 的 clip 裁剪到绘图区，y 轴刻度（x=PAD.L-8）与
    x 轴日期（y=H-PAD.B+18）都在 clip 外被裁
  - 修复：分层绘制——drawAxes 拆分出 drawAxisLabels，drawFuture 拆分出
    drawFutureAxisLabels；drawZoomCanvas 先在 clip 外画坐标轴标签
    （y 范围由 zoomYRange 与各绘制函数一致计算），再 clip 内画数据；
    普通模式走原 drawAxes 不受影响
- 放大后固定直线/窗口随图像移动（用户要求）：
  - 固定直线本就随 ZOOM 重绘跟随；补上 pinTip：拖拽平移与滚轮缩放后
    重新 showPinTip 定位；固定点移出可见范围时隐藏窗口
- 固定窗口拖动碰到图表边界即停止（用户要求）：
  - pinTip 拖动 mousemove 限制在图表边界内（放大模式 zoomCanvas，
    普通模式主画布；放大模式视口坐标，普通模式文档坐标加 scroll 偏移）
- 验证（Edge CDP，40 天模拟数据）：
  - 放大后 y 轴刻度区 77 像素、x 轴日期区 375 像素（坐标轴可见）
  - 锁定 pin.i=24 → 右拖 80px（ZOOM {10,39}→{6,35}）→ pinTip 381.8→469.4 跟随
  - 拖 pinTip 到画布外 → 位置被限制在边界内
  - 无 JS 错误；静态 18 项断言全过

## v0.10.6 (2026-08-11)
- 放大/拖拽时图像不再超出坐标轴（用户要求）：
  - drawZoomCanvas 绘制前对绘图区做 clip 裁剪：
    chart/fit 裁剪 rect(PAD.L, PAD.T, W-PAD.L-PAD.R, H-PAD.T-PAD.B)；
    future 顶部不裁（保留大标题），x/底部按绘图区
  - 拖拽平移后部分蜡烛影线曾画入坐标轴外空白区，现被裁剪干净
- 拖动图像时不触发固定直线/浮动窗口（用户要求，两种情况区分）：
  - 根因：mouseup 先把 _zoomDrag 置 null，随后浏览器触发的 click 判断
    _zoomDrag.moved 恒为 false，误执行锁定
  - 修复：新增 _zoomSuppressClick——mouseup 时若拖拽过（moved）则标记，
    click 消费该标记后跳过锁定；纯点击（未拖拽）不受影响
- 验证（Edge CDP，40 天模拟数据）：
  - 拖拽（右 200px）→ 松手后 click：window._pin 保持 null（不固定）；
    _zoomSuppressClick 消费后 false；纯点击再固定 {view:chart, i:18}
  - 拖拽到边界后绘图区左侧外像素数 = 0（clip 生效，无越界）
  - 无 JS 错误；静态 17 项断言全过

## v0.10.5 (2026-08-11)
- 删除放大时的上下移动功能（用户要求，定位根因后回退）：
  - 用户发现放大横轴同时纵轴也重算导致图像上下抖动
  - 删除 _zoomPanY 纵轴平移（声明/进出归零/绘制与十字线 translate/拖拽 dy）
  - 纵轴范围恢复全局固定：priceMinMax / seriesMinMax / 未来预测 y 范围
    不再随可见范围重算（只缩放横轴，图像稳定不抖）
  - 保留横轴缩放：xOf 可见范围映射、蜡烛宽度按可见天数、日期标签按可见范围
- 验证（Edge CDP，前 20 天 30~55 波动 + 后 20 天 44~46 平稳）：
  - 普通/全放大/缩放到后 20 天，priceMinMax 均为 [27.54, 56.84]（纵轴恒定）
  - 拖拽（右 40px 下 60px）→ 仅横轴 i0 变化，_zoomPanY 不存在（上下移动已删）
  - 放大画布绘制 10822 像素、无 JS 错误

## v0.10.4 (2026-08-11)
- 放大状态下图表可上下移动（用户要求，平滑过渡）：
  - 新增 _zoomPanY 纵轴平移量：drawZoomCanvas 变换加 translate(0,_zoomPanY)，
    drawZoomCrosshair 同步变换（十字线跟随）
  - 左键拖拽同时处理 dx（横轴）与 dy（纵轴），上下移动图表；
    panY 限制在绘图区高度内防止移出画布；进入/退出放大归零
- 当前股标题涨跌幅零延迟（用户要求，不要反应时间）：
  - setChartData 标题设置后立即调用 syncTitleFromWatch()——直接用
    _watchQuotes（自选股列表已有数据缓存）同步，不等网络请求返回；
    数据更新刷新仍由自选股列表的 30 秒定时器负责，标题只做直接显示
- 验证（Edge CDP，40 天模拟数据）：
  - 预置 _watchQuotes 后 doKlineSecid → 同步立即读取标题 = "+1.68%"（零延迟）
  - 放大拖拽（右 30px 下 50px）→ _zoomPanY=73、画布重绘 8318 像素；
    拖回原位 panY=0；退出放大 panY=0
  - 无 JS 错误；静态 18 项断言全过

## v0.10.3 (2026-08-11)
- 修复放大横轴后数据仍按原坐标轴显示（用户报告）：
  - 根因：纵轴范围此前始终按全部数据计算——放大横轴后价格轴不变，
    可见区间内价格变化只占极小高度，蜡烛几乎平铺，数据不符合放大后的坐标
  - 修复：纵轴范围随可见范围（ZOOM）重算——
    priceMinMax / seriesMinMax 只取可见 [i0,i1] 数据求 min/max；
    K线/成交量/涨跌幅蜡烛宽度按可见天数计算（放大后蜡烛变宽）；
    未来预测视图 y 范围只取可见预测点
  - 普通模式（ZOOM null）范围与宽度不变，无行为差异
- 验证（Edge CDP，前 20 天价格 30~55 波动 + 后 20 天 44~46 平稳）：
  - 全局 priceMinMax [27.5, 56.8]；ZOOM {20,39} 后 [43.3, 45.7]（收窄到
    全局 40% 以下）——纵轴随可见数据重算
  - 放大画布绘制 20031 像素、无 JS 错误

## v0.10.2 (2026-08-11)
- 点击图表之外的地方：清除已锁定的参考线与固定窗口（用户要求）
  - document 级 click 监听：点击目标非 canvas/浮窗/关闭叉时，清空
    window._pin、隐藏 pinTip，并按当前状态重绘（放大模式重画放大画布）
  - 点击图表内部锁定照常（canvas click 冒泡被排除，不误清）
  - 放大模式点击遮罩空白同样清除
  - 退出放大（✕）时同步清除锁定参考线
- 固定窗口（pinTip）可鼠标拖动（用户要求）：
  - pinTip 上 mousedown 开始拖动，mousemove 跟随移动，mouseup 停止；
    preventDefault 防止选中文本；拖动结束的 click 不触发清除
- 验证（Edge CDP，40 天模拟数据）：
  - 点击图表锁定 → pin {view:chart,i:19}、窗口显示
  - 点击页面 h2 标题 → pin null、窗口隐藏；再点图表 → 重新锁定正常
  - 拖动 pinTip 40px/25px → left/top 精确跟随
  - 放大模式：点 zoomCanvas 锁定 → 点遮罩空白 → pin null、窗口隐藏
  - 无 JS 错误

## v0.10.1 (2026-08-11)
- 修复放大模式下悬浮窗口消失（用户报告）：
  - 根因：.tip-box z-index 99 低于放大遮罩 #zoomOverlay 的 z-index 1000，
    浮窗 DOM 虽显示但被遮罩盖住
  - 修复：.tip-box z-index 99 → 1002（浮窗始终在遮罩之上，普通模式无副作用）
- 修复固定线条窗口（pinTip）滚动页面遮挡其他信息（用户报告）：
  - pinTip 从 fixed 改为 absolute 相对页面（文档坐标）定位：页面滚动时
    随图表一起移动，不再悬浮在视口固定处遮挡滚动上来的内容
  - 放大模式下 pinTip 动态切回 fixed（与全屏 overlay 视口定位一致）
  - 修复 zooming 判断：style.display !== "none" 对 CSS 控制的 display
    恒为 true（内联未设时为空串）→ 改为 === "flex"
- 验证（Edge CDP，40 天模拟数据）：
  - 放大模式 hover 浮窗 z-index 1002 > 遮罩 1000（可见）；pinTip fixed 显示
  - 普通模式 pinTip position absolute；页面滚动 150px 后其视口 top
    472 → 322（随内容移动），不再遮挡其他信息
  - 无 JS 错误

## v0.10.0 (2026-08-11)
- 新增图表全屏放大（双击任意图表进入）：
  - 双击行情图表/模型拟合/未来预测任一画布 → 全屏放大：半透明遮罩变暗、
    图表自适应占满窗口、右上角 ✕ 关闭
  - 放大状态下功能全部保留：鼠标单击锁定参考线（固定浮窗）、
    悬停浮动窗口、红涨绿跌等绘制不变
  - 滚轮以鼠标位置为中心缩放横轴（可放大查看细节）
  - 鼠标左键按下拖拽图表左右平移（确保缩放后能看到全部信息）
- 实现要点：
  - ZOOM={i0,i1} 可见天数索引范围，xOf/xF 按可见范围映射（普通模式行为不变）
  - drawZoomCanvas 临时把全局 ctx/fctx 切到放大画布（坐标系缩放为 W×H），
    复用全部原绘制函数，矢量重绘不模糊
  - drawAxes 日期标签、未来预测日期标签按可见范围取步长
  - showPinTip/drawPinLine 放大模式定位用 zoomCanvas
- 验证（Edge CDP，40 天模拟数据）：
  - 双击进入：遮罩显示、ZOOM {0,39}、画布绘制 15896 像素
  - 滚轮：span 39 → 29.25（以鼠标为中心）
  - 单击锁定：pin {view:chart, i:19}、固定浮窗显示；悬停浮窗显示
  - 拖拽：i0 5 → 2（视图平移）；关闭后遮罩隐藏、ZOOM null
  - fit 视图 ZOOM {0,39} 绘制 12804 像素；future 视图 ZOOM {0,9}
  - 无 JS 错误；静态 21 项断言全过

## v0.9.12 (2026-08-11)
- 当前股标题涨跌幅不再单独抓取（用户要求，此前"不是不一样就是不显示/读取不到"）：
  - refreshAllQuotes 只请求自选股 secids（不再并入 _curSecid），删除
    updateCurQuoteFrom 独立更新路径
  - 新增 syncTitleFromWatch：标题涨跌幅直接从自选股列表数据行同步——
    自选行有有效行情时标题显示同一数值（同值同色），自选行无数据/当前股
    不在自选列表时标题显示灰色 —，与自选股行状态永远一致
  - renderWatchQuotes 每次渲染自选行后自动同步标题
- 验证：
  - Edge CDP：注入模拟行情 +1.68% 与 -1.23% 两处文本与颜色一致；
    当前股不在自选列表（_watchQuotes 无数据）标题 —；
    请求失败（_watchQuotes 空）标题 —
  - 静态 14 项断言全过（只请求自选股、无 updateCurQuoteFrom 残留、
    syncTitleFromWatch 联动、标题占位 —、无 K线兜底、视图重绘、自动下载等回归）

## v0.9.11 (2026-08-11)
- 修复未来预测模块点击自选股不切换（用户报告）：
  - 根因：setChartData 切换股票后只调 paint(currentType) 重绘行情画布——
    future 视图下 futureChart 与 futurePanel 不重渲染，仍显示旧股票数据；
    fit 视图下指标面板同样不刷新（画布靠鼠标移动触发 drawFit 兜底才更新）
  - 修复：setChartData 末尾按当前视图分支完整重绘——fit 视图
    renderFitPanel+drawFit，future 视图 renderFuturePanel+drawFuture，
    chart 视图 paint(currentType)；同时清除失效固定参考线（window._pin）
- 验证（Edge CDP，模拟双股数据注入 localStorage 缓存）：
  - future 视图点击中国神华 → D 切到神华、面板显示 44.x 预测值、
    无茅台 131x 残留
  - fit 视图点击贵州茅台 → D 切到茅台、面板 MAE/RMSE 渲染、
    画布绘制 20174 像素
  - chart 视图点击切换正常、无 JS 错误

## v0.9.10 (2026-08-11)
- 修复当前股标题涨跌幅与自选股列表数值不一致（用户实测报告"依旧不同"）：
  - 根因：标题此前用 K线最后交易日涨跌幅兜底（数据源滞后/盘中时是昨天或上一交易日
    的数值），而自选股行用实时批量行情——两处数据源不同，数值必然不同
  - 修复：标题涨跌幅完全由 refreshAllQuotes 的实时批量行情驱动——与自选股列表
    同一接口（/api/quotes）、同一时刻、同一份 map，两处显示绝对同源一致；
    实时数据未返回时标题显示灰色"—"占位，不再展示不同源的 K线旧值
  - updateCurQuoteFrom 填充时恢复醒目样式（font-weight 600 覆盖占位浅重）
- 新增：点击候选旁 ＋ 加入自选后，自动下载该股K线并打开图表（命中缓存秒开），
  无需再点候选按钮
- 验证：
  - 静态 10 项断言全过（无 K线兜底代码、占位"—"、同 map 驱动、addWatch 调
    doKlineSecid、候选保留、30 秒定时器）
  - Edge CDP 注入模拟数据：+1.68% 与 -1.23% 两处文本与颜色均一致
  - Edge CDP 真实东财数据：标题 -0.36%（绿）与自选行 -0.36%（同色）完全一致
  - 加入自选命中缓存自动打开图表（status="已用今日缓存数据"、标题渲染该股）

## v0.9.9 (2026-08-11)
- 当前股标题涨跌幅与自选股列表涨跌幅完全同源一致：
  - refreshAllQuotes 把当前股（_curSecid）并入批量请求（/api/quotes，一次请求同一时刻），
    返回的同一份 map 同时驱动标题涨跌幅（updateCurQuoteFrom）与自选股行渲染
  - 删除独立的 /api/quote 前端请求（updateCurQuote）——此前标题用单只接口、自选股用
    批量接口，请求时刻/数据源不同导致数值不一致
  - 30 秒定时器统一刷新两处，刷新频率一致；当前股不在自选股时也随批量请求更新
  - K线最后交易日涨跌幅仍作为标题兜底（实时数据无效时保留，不闪变）
- 验证：模拟数据 Edge CDP —— 正涨 +1.68% 两处文本一致、负涨 -1.23% 两处文本与
  颜色（绿 rgb(8,153,129)）均一致、行情缺失时标题保持原值；16 项 ad-hoc 断言全过
  （含 refreshAllQuotes 并入当前股、同一 map 驱动、无独立 /api/quote 请求）

## v0.9.8 (2026-08-11)
- 修复候选选项闪烁消失：setChartData 不再清空 candidateBox（此前打开任何股票图表
  都会清空候选，用户尚未加入自选股时候选就消失了）
  - 现在候选只在重新搜索时替换（doSearch 清空重渲染）；点候选打开图表、点 ＋ 加入
    自选、点自选股行打开图表均保留候选，直至用户主动再次搜索
  - 搜索失败时保留旧候选（容错）
- 验证：Edge CDP 全流程 —— 搜索 601088 候选"中国神华"出现；点候选打开图表后
  cands:1（保留）；点＋加入自选后 cands:1；点自选股行打开后 cands:1；
  curStock 标题正常更新（+0.34% → +0.29% 实时刷新）

## v0.9.7 (2026-08-11)
- 自选股实时行情删除 成交量/成交额 两列（数据源异常时该字段常为空/0，查询不到
  不再保留空列）：表头行、数据行、renderWatchQuotes 填充逻辑同步移除，
  清理 .wl-vol/.wl-amt 样式与无引用的 fmtVol/fmtShares 函数
- 当前股标题涨跌幅数据源统一：先用K线最后交易日涨跌幅兜底，随后异步请求
  /api/quote 用实时行情刷新（新增 updateCurQuote，与自选股实时行情同源，
  数值一致）；实时接口无效时保留K线兜底值
- 当前股醒目标题移出 chartControls，独立为 .cur-stock-line 行（tabs 上方），
  三个视图（行情图表/模型拟合/未来预测）均显示，不再随视图切换隐藏
- 验证：Edge CDP —— 表头"名称最新价涨跌幅昨收PEPB总市值"（无成交量/成交额）、
  数据行 8 列；三个视图 curStock 均显示（chart/fit/future 全 true）；
  updateCurQuote 实测 +1.68%(K线兜底) → +0.58%(实时) 刷新成功

## v0.9.6 (2026-08-11)
- 当前查询股票醒目标题：图表区上方大字显示 股票名称 + 代码 + 最新涨跌幅
  （红涨绿跌，取自K线最后交易日）；未查询时显示"尚未查询股票"（灰色小字）
- 自选股列表新增表头行：名称 / 最新价 / 涨跌幅 / 昨收 / 成交量 / 成交额 /
  PE / PB / 总市值（灰字小号，与数据列对齐）；同时修复表头行 wl-x 空占位
  点击会把全部自选股误删的隐患（removeWatch 前校验 data-rm）
- 实时行情无效值过滤（杜绝显示 000）：
  - 后端：东财 ulist 批量 / stock 单只 / 新浪 hq 三条路径全部丢弃 price 为
    None/0/非数字 的条目（新浪当前现价字段偶发返回 0.000，昨收正常）
  - 前端：price 无效时该行不渲染、保持"正在查询数据"；成交量/成交额/昨收/
    总市值 <=0 显示"—"；涨跌幅无效显示"—"
- 验证：node --check + Edge CDP 实测 —— 表头行渲染（名称最新价涨跌幅昨收
  成交量成交额PEPB总市值）、当前股标题"中国神华 601088 +1.68%"(红色)、
  price=0 注入后行保持"正在查询数据"、有效数据渲染 44.68/+1.68%/22.71；
  22 项 ad-hoc 断言全过；当日东财 push2 持续 RemoteDisconnected、新浪现价 0
  时服务端正确返回空数组（不造假数据），前端行保持查询中而非 000

## v0.9.5 (2026-08-11)
- 删除顶部独立实时行情条（#quoteBar）：当前查询股票的实时行情不再单独占一行，
  股民只通过自选股列表一次性查看所有自选股实时行情
  - HTML/CSS/JS 全量清理（.card-bar/.qb-* 样式、renderQuoteBar 函数、refreshAllQuotes
    中当前股并入批量请求的逻辑、_curSecid 变量）
  - refreshAllQuotes 只请求自选股 secids；失败提示去掉"点行情条 ↻ 手动刷新"
- 自选股行加载状态：数据未返回时价格位显示"正在查询数据"（灰色）而非横线"—"；
  数据到达后自动填充实际价格（红涨绿跌）；请求失败时保持"正在查询数据"，
  30 秒定时器自动重试，不显示假数据
- 验证：node --check 模板 JS 通过；Edge CDP 注入两只自选股 → 初始行显示
  "正在查询数据"（非"—"）→ 行情返回后填充 "44.68"；quoteBar 元素不存在；
  WSL 东财接口抖动时新浪回退正常工作（当日实测东财 push2 持续 RemoteDisconnected，
  新浪 hq 备用源成功返回）

## v0.9.4 (2026-08-10)
- 自选股实时行情改为"每只单独一行"：每行显示 名称+编号 / 现价(红绿) /
  涨跌幅 / 昨收 / 成交量 / 成交额 / PE / PB / 总市值 / 删除×，
  点击整行打开该股图表
- 所有自选股同一时间刷新：一次批量请求（/api/quotes）返回全部，
  renderWatchQuotes 同步更新每一行（30 秒定时器统一触发）
- 样式：白卡片 + 行分隔线 + hover 高亮，列宽对齐
- CDP 验证：三只自选各一行，数据齐全
  （中国神华 44.68 +1.68%；贵州茅台 1348.86 +3.03%；
   招商银行 38.91 +0.28%，量额齐全）

## v0.9.3 (2026-08-10)
- 自选股列表移回查询区下方（候选框之后），横向排列可换行
- 修复实时行情加载不出的两个问题：
  a) fetch_quote 单只返回缺 secid 字段 → 新浪回退时前端按 secid 匹配
     chips 失败，所有价格不显示 → 东财/新浪两条路径都补 secid
  b) 行情类接口重试 4 次→2 次，东财失败更快切换新浪备用源
- 修复"过一会不能查询"：原 pagehide 立即 shutdown 会在刷新/拖拽标签页
  时误杀服务 → 改为延迟退出：shutdown 只打标记，30 秒内无任何请求
  才退出；任何请求自动取消待执行退出（刷新后页面加载即发请求，
  不会误停）。实测：标记后 3 秒仍服务正常、发请求取消退出、
  30 秒无请求端口释放进程归零
- 实时行情失败时状态栏提示"实时行情获取失败，30 秒后自动重试，
  或点行情条 ↻ 手动刷新"；行情条新增 ↻ 手动刷新按钮
- CDP 验证：自选股位于候选框之后（查询下方）；两只自选价格齐全
  （"44.68 +1.68%"、"1348.86 +3.03%"）

## v0.9.2 (2026-08-10)
- 自选股实时行情：每只自选 chip 上直接显示 现价 + 涨跌幅（红涨绿跌），
  有几个显示几个
- 基本面数据合并进实时行情条（一行）：PE(动)/PB/总市值/流通市值
  （东财 ulist 批量接口提供；原独立 fundBar 已删除）
- 统一刷新：当前股 + 全部自选股一次批量请求（/api/quotes 新路由，
  东财 ulist 批量 → 失败回退逐只新浪），30 秒定时器同一时刻刷新全部
- 新加自选后立即触发一次刷新，拉取该股价格
- 修复：新增自选后价格不显示（addWatch 后补 refreshAllQuotes）
- 验证：批量接口三只股票（中国神华/贵州茅台/招商银行）价格+PE+PB+
  市值齐全；CDP 实测 chips 显示"44.68 +1.68%"、"1348.86 +3.03%"；
  quoteBar 一行含 PE(动)22.71/PB2.11/总市值9690.84亿/流通市值7368.20亿

## v0.9.1 (2026-08-10)
- 实时行情条前面加股票名称 + 编号（如"中国神华 601088"，名称加粗、
  编号灰色小字）
- 自选股列表移到"查询股票"行右侧空白处（flex:1 自动占满右侧，
  一行放不下自动换行）
- K线缓存机制：已下载过的股票（含自选股）当日不再重复下载——
  localStorage 缓存 key 含 secid+日期范围，24 小时有效；
  命中缓存时状态栏提示"已用今日缓存数据（无需重新下载）"；
  缓存超过 20 个自动清理最旧一半
- CDP 验证：自选股位于查询行内、点击自选股命中缓存秒开
  （status="已用今日缓存数据"）、chips 正常

## v0.9.0 (2026-08-10)
- 新增三大功能（面向股民）：
  1. 实时行情自动刷新：查询后页面顶部显示行情条（最新价大字/涨跌幅红绿/
     昨收/成交量/成交额/更新时间），每 30 秒自动刷新；
     数据源东财 push2 实时接口（主）+ 新浪 hq 接口（备）
  2. 自选股列表：候选旁"＋"加入自选（localStorage 持久化），
     页面上方胶囊 chips 显示，点击直接打开该股图表，× 删除
  3. 基本面面板：图表下方显示 PE(动)/PB/总市值/总股本/流通股本/每股净资产
     （东财源完整；新浪备用源字段显示 —）
- 后端：新增 /api/quote 路由 + fetch_quote 双源（东财→新浪）；
  修复 parse_qs 返回 list 导致 secid.split 崩溃的 bug
- CDP 验证：实时行情条（44.68 +1.68% 量额齐全、30 秒刷新提示）、
  基本面条显示、自选股增删查通过（localStorage 同步）

## v0.8.1 (2026-08-10)
- 行情图表模块：删除右侧常驻数据面板（#tip 及 260px 侧栏）
- 图表横向扩大：canvas 宽度 900 → 1140（三个视图共用，全部变宽）
- 行情图表数据改为浮动窗口显示：鼠标悬停时浮窗跟随鼠标显示
  日期/开盘/收盘/最高/最低/成交量/涨跌幅/MA5/BOLL（复用 tip-box 全局样式）
- 清理：#tip CSS/HTML、switchView 引用、doKline 残留提示、重复 CSS 行
- CDP 验证：canvas 1140 宽、右侧面板不存在、浮动窗口数据正常
  （"日期2026-02-03 开盘40.25 收盘39.60 ..."）

## v0.8.0 (2026-08-10)
- 三个模块（行情图表/模型拟合/未来预测）新增"点击固定参考线"：
  - 单击图上某天 → 固定一条垂直虚线 + 固定悬浮窗口（显示该日数据），
    最多一条；再点其他位置即重选固定线
  - 鼠标继续移动时，原有跟随浮窗（hover）照常显示，两者并存
- 实现：统一 tipContentFor(view, i) 生成三视图浮窗内容；
  showPinTip 固定浮窗定位；drawPinLine 三视图绘制固定线；
  cv.onclick / fcv.onclick 绑定点击；switchView 切视图时同步固定浮窗
- 验证：node --check 模板 JS 语法通过；20 项断言全过
  （函数定义/绑定/三视图固定线接入/浮窗样式全局类）
  （CDP 真实点击验证受 WSL 网络抖动影响未稳定复现，代码路径与已验证
  的 hover 浮窗同一函数体）

## v0.7.6 (2026-08-10)
- 未来预测图新增十字线 + 悬浮窗口（同模型拟合板块交互）：
  - 鼠标移到图上某天 → 垂直虚线十字线 + 该日点高亮
  - 浮窗显示：日期、真实收盘(现价)、5 模型预测值（色块标注）、
    最终(加权)预测值
  - 浮窗样式提取为全局 .tip-box 类（模型拟合/未来预测共用）
- CDP 验证：浮窗 7 行内容
  "2026-08-15 现价44.68 / ARIMA44.74 / ETS44.93 / Prophet41.97 /
   SVR45.51 / 随机森林43.87 / 最终(加权)44.28"

## v0.7.5 (2026-08-10)
- 未来预测图删除文字标签（美观）：
  - 删除"现价 xx.xx"标签（保留黑色虚线现价基准线）
  - 删除"最终 xx.xx"末端标签（每点已有数值标注）
- 顶部三个板块按钮（行情图表/模型拟合/未来预测）与下方图表左对齐：
  - .tabs 原为全宽靠左（起点 20px），.wrap/.row2 为 max-width 1180 居中
    （视口宽时起点 >20px）→ 不对齐
  - .tabs 改为 max-width: 1180px; margin: 0 auto 10px 与图表同宽同居中
- CDP 验证：tabs 左端 20px = 图表容器左端 20px（aligned true）

## v0.7.4 (2026-08-10)
- 修复未来预测面板样式失效的根因：图例/逐日行复用的 .f-row/.f-name/
  .swatch/.f-pred 等样式全部限定在 #fitPanel 作用域，futurePanel 元素
  吃不到 → 布局乱、色块无尺寸（看起来只有文字）、日期与价格连排
  → 样式全局化（去掉 #fitPanel 前缀），两板块共用同一套样式
- 色块加大：swatch 18×4 → 20×10 圆角矩形（颜色+文字更明显）
- 逐日行优化：日期与价格 flex 两端分开；价格加粗（.val font-weight:700），
  与日期有粗细区别
- CDP 验证（未来预测面板）：图例行 flex、色块 20×10、逐日行 flex、
  价格粗体 700、5 模型名齐全

## v0.7.3 (2026-08-10)
- 未来预测图例统一为"色块 + 模型名"（同模型拟合板块样式）：
  ARIMA 预测 / ETS 指数平滑预测 / Prophet 预测 / SVR 预测 / 随机森林预测
- 删除"停止服务"按钮，改为关闭页面即停止进程：
  - 页面卸载（pagehide）时 navigator.sendBeacon("/api/shutdown") 通知服务
  - 后端新增 do_POST 处理 shutdown（sendBeacon 走 POST）
  - 实测：页面导航离开后服务 3 秒内自动退出，端口释放，无残留进程
- 清理 stop-btn CSS、启动提示语更新（"关闭浏览器页面即停止服务"）

## v0.7.2 (2026-08-10)
- 未来预测右侧图例补齐：
  - 新增"最终预测（RMSE 逆加权平均，粗线）"行（黑色粗条，对应图上
    加权平均粗线——此前图例只有 现价+5模型，黑色被"真实收盘"占用，
    最终预测线无法对应）
  - 真实收盘改为虚线样式标注（对应图上黑色虚线现价基准线）
  - 图例共 7 行：现价虚线 / 5 模型色条 / 最终预测粗黑条
- CDP 验证：图例 7 行、虚线标注、最终预测行、5 模型齐全

## v0.7.1 (2026-08-10)
- 未来预测模块重构：
  - 图例移到右侧面板（仿模型拟合：色块 + 名称，6 行：
    真实收盘现价 + 5 模型），删除画布右上角图例
  - 删除每个模型的逐日预测行（原 50 行 → 只保留最终预测 10 行）
  - 新增"最终预测（RMSE 逆加权平均）"：5 模型按 1/RMSE 加权，
    误差小的模型权重大；图上画黑色粗线（2.8px + 大圆点 + 每点数值 +
    末端"最终 xx.xx"标签），模型线改为细线（2px + 小圆点）不标数值
  - 右侧面板：图例 + 最终方向（上涨↑/下跌↓ + 10日后值 + 累计涨跌%）
    + 逐日 10 行（日期 + 加权值 + 相对现价涨跌%）
- CDP 验证：图例 6 行、预测行 10（仅加权平均）、无旧版逐日、
  加权线渲染 423260 像素
- 补充健壮性：rmse 保底 1e-9 + 前端过滤 rmse>0 —— 拟合完美的模型
  （rmse 四舍五入为 0）曾导致 1/0=Infinity，加权失稳

## v0.7.0 (2026-08-10)
- 模型拟合模块新增三个模型（全部纯 Python 轻量版，零第三方依赖）：
  - Prophet(轻量)：线性趋势 + 3 变点分段斜率 + 傅里叶周度季节，
    一次最小二乘解出（核心思想同 Meta Prophet，不装 fbprophet 重依赖）
  - SVR(核岭回归)：RBF 核 K_ij=exp(-γ(t_i-t_j)²)，Cholesky 解 (K+λI)α=y
  - 随机森林(轻量)：手写决策树回归 + bagging（30 棵 × 随机 60% 子样本，
    深度 8，最小化 MSE 分裂）
- 三模型接入拟合视图（拟合线 + MAE/RMSE/R²）与未来预测视图
  （预测线 + 10 日逐行 + 方向 + 涨跌%），共 5 个模型：
  真实收盘(黑) / ARIMA(红) / ETS(绿) / Prophet(橙) / SVR(紫) / RF(青)
- 实测（601088 近一年）：拟合耗时 0.3s；Prophet R²=0.65、SVR R²=0.76、
  RF R²=0.98（树拟合贴合但预测为水平外推，属树模型特性）；
  CDP 验证拟合面板 5 模型行、未来预测 50 行（5×10）、图例含新模型

## v0.6.2 (2026-08-10)
- 修复模型拟合图残留未来预测：v0.5.7 拆出独立"未来预测"视图时，
  drawFit() 里的未来预测绘制（虚线延伸、未来区背景、"未来5日预测"标注、
  预测图例项）未同步移除，一直叠加在拟合图上
  → drawFit 恢复为纯拟合线（真实收盘 + ARIMA + ETS 三条实线），
  删除 drawAxesFit 函数；图例只含三项
- 后端注释同步（"未来5日预测"→"未来10日预测"，纯 Python 零依赖）
- CDP 验证：拟合图图例仅 真实收盘/ARIMA/ETS；未来预测视图独立正常
  （425001 像素、20 行、10日后方向）

## v0.6.1 (2026-08-10)
- 模型拟合视图右侧面板删除"未来5日预测"区块（标题仍显示 5 日但数据已 10
  日，且未来预测已有独立视图，属冗余）：
  - 面板只保留各模型 MAE / RMSE / R² 指标
  - 清理不再使用的 .f-pred-title CSS
  - 未来预测视图不受影响
- CDP 验证：拟合面板无"未来5日预测"/方向行，MAE/R² 保留

## v0.6.0 (2026-08-10)
- 模型拟合视图新增悬停浮动窗口（仅拟合视图，未来预测视图不做）：
  - 鼠标移到拟合图上某天 → 鼠标旁浮动窗口显示：
    当日日期、真实收盘价、ARIMA 拟合值（红）、ETS 拟合值（绿）
  - 窗口跟随鼠标，越界自动翻转到另一侧；鼠标移出画布自动隐藏
- 行情视图（chart）保持原有右侧面板 tooltip 不变
- 实测（601088）：CDP 验证浮窗内容
  "2026-01-27 真实收盘40.65 / ARIMA 41.55 / ETS 41.64"

## v0.5.9 (2026-08-10)
- 修复 ARIMA 预测永远平线：原实现为无常数项 AR(1) on 差分，股票日涨跌
  无自相关 → φ≈0 → 预测恒等于最后收盘价
  → 改为 ARIMA(1,1,0)+drift（含漂移项 c=平均日涨跌，φ 用中心化最小二乘
  估计），预测反映历史趋势斜率（实测 601088：43.96→44.13 缓升）
- 修复未来预测日期错误：原从"最后交易日"顺延，数据源滞后（新浪回退）
  时会出现已过去的日期（如 08-08）
  → 一律从今天起算（实测预测区间 2026-08-11 ~ 08-20）
- 图像优化：大标题下加副标题"今日 xx ｜ 预测区间 xx ~ xx"；每条预测线
  末端加"10日后 xx.xx"大标签（15px 粗体）；两条线方向不同（ARIMA 微涨
  / ETS 微跌）区分明显
- CDP 验证：未来预测画布 424970 像素、预测行 20、首行 08-11 44.68

## v0.5.8 (2026-08-10)
- 修复：模型拟合视图图消失 —— v0.5.7 切换逻辑把 chart 画布只在"行情图表"
  视图显示，而拟合图（drawFit）用的正是同一画布，导致拟合视图画布被隐藏
  → chart 在 行情图表 与 模型拟合 两个视图都显示，仅"未来预测"视图隐藏
- CDP 验证：拟合视图 chart 显示且绘制 47626 像素、未来视图切换正常

## v0.5.7 (2026-08-10)
- 未来预测从 5 日改为 10 日（compute_fits 增加 horizon 参数，默认 10）
- 新增独立"未来预测"视图（第三个标签）：
  - 独立画布 futureChart（不再与拟合图共用）
  - 画得醒目：大标题"未来 10 日预测"（20px 粗体）、淡黄未来区背景、
    3px 粗预测线 + 白边圆点 + 每个点上方数值标注、现价黑色虚线基准线
    + "现价 xx.xx"标签、图例绘制在画布右上角
  - 右侧面板：每个模型 10 日方向（上涨↑/下跌↓ + 累计涨跌%）、
    10 行逐日预测（日期 + 预测值 + 相对现价涨跌%）
- 实测（601088）：ETS 线性外推 10 日正确；CDP 验证未来预测画布
  422631 像素、预测行 20（2模型×10日）、方向与百分比齐全、
  画布切换（chart 隐藏 / futureChart 显示）正常

## v0.5.6 (2026-08-10)
- 修复"模型拟合不可用"：compute_fits 改为纯 Python 实现，零第三方依赖
  （仅需 requests），彻底移除 numpy/statsmodels 依赖
  - 线性回归：最小二乘手写公式
  - ARIMA(1,1,0)：差分 + AR(1) 系数手写
  - ETS：Holt 网格搜索改 range 循环
- 根因：用户 VSCode 环境未装 numpy，旧实现 import numpy 失败返回 None
  （此前 statsmodels 版同样因依赖缺失失效）
- 验证：强制阻断 numpy 导入后 compute_fits 仍正常（线性外推 51~55、
  601088 R² 与 numpy 版一致 ARIMA 0.9524 / ETS 0.9491）
- 排查中发现残留旧服务进程（系统 python 无 numpy）占旧端口导致假象，
  已清理；页面 CDP 验证无"不可用"提示、2 模型、10 行预测、方向标注齐全
- 补充健壮性：compute_fits 过滤 None/非法收盘值并同步日期（此前含 None
  会 float() 崩溃；验证 None 被剔除、全 None 返回 None、短序列返回 None）

## v0.5.5 (2026-08-10)
- 模型拟合视图新增"未来5日预测"：
  - ARIMA(1,1,0)：差分 AR(1) 递推外推 5 日
  - ETS：Holt 最优 α/β 递推至末尾状态，水平+趋势外推 5 日
  - 图上未来区：浅灰背景 + 垂直虚线分隔 + "未来5日预测"标注，
    预测线为虚线（与实线拟合区分），x 轴标未来日期（MM-DD）
  - 右侧面板每个模型增加：预测区间、5日后方向（上涨↑/下跌↓，红/绿）、
    逐日预测值（日期+价格，纵向 5 行）
- 后端 compute_fits 增加 dates 参数与 predict/predict_dates 字段，
  /api/kline 传入实际交易日
- 实测（601088 最后收盘 43.94）：预测日期 08-08~08-12；CDP 验证
  预测区渲染（46325 像素）、预测行 10（2模型×5日）、方向行 2、图例含
  两条虚线预测项

## v0.5.4 (2026-08-10)
- 模型拟合图删除线性回归线（保留 真实收盘 + ARIMA + ETS），
  右侧指标面板同步移除线性回归行
- 修复：拟合视图下鼠标移动触发 mousemove 处理器无条件调用 paint(currentType)，
  把拟合图重绘成 K 线图
  → 按当前视图分支：view=fit 时重画 drawFit()，否则 paint(currentType)
- 附带排查：发现此前 /api/shutdown 未能真正停止服务（残留旧进程监听旧端口，
  导致页面加载旧模板的假象），本次直接 kill 进程清理；shutdown 机制
  保留但已知可能不彻底
- 实测：CDP 验证 mousemove 后画布仍为拟合图（38969 像素、图例保持
  "真实收盘/ARIMA/ETS"），指标块 2 个、无线性回归

## v0.5.3 (2026-08-10)
- 拟合引擎替换：去掉 statsmodels 依赖（体积大、环境易缺失导致"模型拟合不可用"），
  改为纯 numpy 实现，任何环境开箱即用：
  - 线性回归：最小二乘直线
  - ARIMA(1,1,0)：一阶差分 + AR(1) 系数最小二乘估计（数学等价，φ 限幅保平稳）
  - ETS：Holt 线性趋势指数平滑，α/β 网格搜索最小化 SSE（α=0.05~0.95, β=0.01~0.3）
- 前端不可用提示改为"数据不足或拟合失败"
- 实测（601088 近一年 236 交易日）：耗时 0.1s（原 0.9s），ARIMA R²=0.9524 /
  ETS R²=0.9491 / 线性回归 R²=0.4865，与 statsmodels 版结果几乎一致；
  CDP 验证拟合视图渲染正常（40394 像素、3 指标块）

## v0.5.2 (2026-08-10)
- 修复：拟合视图切换时 canvas 未清空，残留上一张 K 线图叠加在拟合折线上，
  看起来像"K线图"
  → drawFit() 开头增加 ctx.clearRect；验证切换后纯折线渲染 37877 像素

## v0.5.1 (2026-08-10)
- 新增"模型拟合"视图（页面顶部标签切换：行情图表 / 模型拟合）
  - 同一张图绘制：真实收盘价（黑）+ ARIMA(1,1,0)（红）+ 线性回归（蓝）
    + ETS 指数平滑（绿），图例在图上标注
  - 右侧面板纵向列出每个模型的 MAE 平均绝对误差 / RMSE 均方根误差 /
    R² 拟合度（模型色块标注，一行一个模型）
  - 拟合基于所选时间段的真实收盘价（in-sample）
- 后端 compute_fits：statsmodels ARIMA/ETS + numpy 最小二乘线性回归；
  依赖缺失或拟合失败时 fit 返回 null，前端提示"不可用"，不影响行情
- 修复两个 bug：
  1. np.polyfit 返回 [斜率, 截距]，原代码赋值反了导致线性回归拟合线
     斜率爆炸（MAE 4598 → 修复后 2.04）
  2. ARIMA 拟合首值为 NaN，json.dumps 输出非标准 JSON，浏览器解析失败
     → NaN 统一转 None
- 实测：601088 近一年 236 交易日，ARIMA R²=0.9526 / ETS R²=0.953 /
  线性回归 R²=0.4865；CDP 验证拟合视图渲染 48389 像素、3 模型指标块齐全、
  切回行情视图正常；statsmodels 经清华镜像安装（官方源超时）

## v0.5.0 (2026-08-10)
- 交互改为浏览器界面：启动后自动打开本地页面（http://127.0.0.1:<随机端口>/）
  - 页面上方"查询股票："输入框 → 输入名称/代码 → 候选按钮 → 点击后
    在同一个页面显示图表（不刷新、不跳转）
  - 保留时间范围选择（date 控件，默认近一年）与 5 种图表下拉切换
- 架构：本地 HTTP 服务（标准库 ThreadingHTTPServer，无第三方框架）
  - / 查询页，/api/search 搜索，/api/kline K线+指标，/api/shutdown 停止服务
  - 浏览器 fetch 本地后端，后端转发东财/新浪（绕过浏览器 CORS 限制）
- 健壮性：写响应捕获 BrokenPipeError（客户端断开不崩进程）、请求线程守护化、
  前端 fetch 90 秒超时（AbortController）、后端重试收紧到 30 秒内
- 数据层（搜索/K线/指标/新浪回退）保持不变，复用 v0.4.x 逻辑
- 实测：Edge CDP 完整交互 输入 601088 → 候选中国神华 → 同页出图
  236 个交易日；5 图切换渲染正常（kline 37894 / ma5 29066 / boll 125775 /
  vol 102033 / change 43841 像素）；/api/shutdown 优雅停止

## v0.4.4 (2026-08-10)
- 新增时间范围询问：下载前询问起始/结束日期（YYYY-MM-DD）
  - 直接回车使用默认（近一年）
  - 带格式校验（非法日期重问）与顺序校验（起始不能晚于结束）
- 新浪备用源数据量随区间动态调整（datalen 300~1023，覆盖更长历史）
- 实测（601088 中国神华）：默认区间 236 条；自定义 2026-01-05~08-10 144 条；
  错误输入提示正常

## v0.4.3 (2026-08-10)
- 网络加固（解决 VSCode 运行时"接口请求失败"）：
  1. 请求重试 3 → 5 次，指数退避（2s/3s/5s/8s/12s），改用 requests.Session 复用连接
  2. K线接口新增新浪备用源：东财（push2his）失败自动回退新浪
     CN_MarketData.getKLineData（A股）；美股/港股保持东财重试
- 新浪回退字段处理：成交额/换手率置空；涨跌额/涨跌幅/振幅按收盘价推算；
  成交量股→手换算；名称经 hq.sinajs.cn 接口获取
- 根因说明：WSL 网络间歇抖动（东财接口偶发 RemoteDisconnected），
  非代码错误，重试+备用源可自动恢复
- 实测：东财抖动时自动回退新浪，241 条日K 正常出图

## v0.4.2 (2026-08-10)
- 数据面板从图表下方移到右侧（.wrap 改 flex 布局：左 canvas + 右信息面板）
- 信息面板竖向排列 11 行（日期/开盘/收盘/最高/最低/成交量/涨跌幅/MA5/BOLL上/中/下），
  字号 13px → 14px，标签灰 / 数值加粗
- 修复重要 bug：模板里 {{ }} 预留转义未转回，导致 CSS 从未生效
  （此前所有版本页面都是无样式渲染；JS 因 {{ }} 被当嵌套块侥幸不报错）
  → build_chart_html 输出前统一转单花括号
- canvas 宽度 1140 → 900 并加 max-width:100% 自适应（窄窗口不挤压信息面板）
- 验证：Edge CDP 渲染无 JS 错误；tip 面板 11 行竖向、font-size 14px 生效、
  canvas 内部绘制 33752 像素（headless 视口 360 下 flex 挤压属预期，真实窗口正常）

## v0.4.1 (2026-08-10)
- K线图去掉叠加指标，改为纯蜡烛图（清晰度优化）；MA5/BOLL 保留独立选项
- 高清屏适配：canvas 按 devicePixelRatio 放大，高分屏不再模糊
- 网格线淡化、Y轴字号增大
- 鼠标悬停：重绘当前图 + 虚线十字线定位当日
- 蜡烛宽度略增（0.62 → 0.68）
- 验证：Edge CDP 实际渲染 5 种图全部成功、无 JS 错误（kline 39346px /
  ma5 32284px / boll 151659px / vol 97661px / change 46758px）

## v0.4.0 (2026-08-10)
- 不再把 CSV 保存到桌面；CSV 改为 data/ 目录留档
- 新增 HTML 图表页（自包含单文件，原生 Canvas 绘图，无外部依赖，离线可开）：
  下拉列表切换，每次显示一张图：
  1. K线图（叠加 MA5/MA20/BOLL 上下中轨）
  2. 5日均线图（收盘价 + MA5）
  3. 布林带 BOLL 图（上/中/下轨 + 区间填充）
  4. 成交量 VOL 图（红涨绿跌）
  5. 涨跌幅柱状图（含 0 轴）
- 指标计算在 Python 侧完成（MA5/MA20/BOLL），JS 只负责绘图
- 鼠标悬浮显示当日 OHLCV/MA/BOLL 明细
- 自动用 Windows 默认浏览器打开（explorer.exe + UNC 路径）
- 实测：茅台 242 条日K → data/chart_*.html 生成并在 Edge 中打开

## v0.3.0 (2026-08-10)
- 数据源替换：investing.com（Cloudflare 防护，requests 与 Edge 无头均 403）
  → 东方财富公开接口（无 token、无防护，A股/美股/港股全覆盖）
- crawler_stock.py 重写为单文件下载器：
  输入名称/代码 → 搜索候选（suggest API）→ 多候选交互选择 →
  下载近一年日K（kline API，前复权）→ CSV 保存到 Windows 桌面
- CSV 列：日期/开盘/收盘/最高/最低/成交量/成交额/振幅/涨跌幅/涨跌额/换手率，
  utf-8-sig 编码，Excel 直接打开不乱码
- 删除 Edge CDP 内嵌脚本（不再依赖 PowerShell / Edge / spider_framework）
- 实测：输入"茅台" → 贵州茅台 242 条日K → 桌面 CSV 生成成功

## v0.2.0 (2026-08-10)
- 新增 crawler_stock.py（单文件自包含）：定向爬取 cn.investing.com/equities
  - 爬取前交互询问股票名称；搜索多候选时让用户确认选择
  - 反爬方案：Windows Edge 无头 + CDP（复用 spider_framework 已验证通道），
    内嵌 PowerShell 脚本写入 %TEMP%，单文件即可运行，不依赖外部脚本
  - 解析：优先 #__NEXT_DATA__ JSON 递归提取，回退常见 DOM 选择器
  - 结果打印 + 存 data/investing_<名称>_<时间>.json
- 按约定未实测：选择器基于站点常规结构，首次运行解析为空时需抓
  实际 HTML 微调（代码内有说明）

## v0.1.0 (2026-08-10)
- 建立通用爬虫框架 crawler/core.py：
  - HttpClient：UA 随机伪装、失败重试（指数退避）、请求限速、本地缓存
  - 支持 get_json / get_text / get_soup 三种取数方式
- crawler/parsers.py：宽松 JSON 解析（容忍注释/尾逗号）、HTML 表格转二维列表
- examples/demo.py：演示请求 JSON 接口、网页抓取、缓存命中
- 修复：缓存回读时正文分段错误（partition 替代 split）
- 本次未接入任何股票数据源，纯通用框架

